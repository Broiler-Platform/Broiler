# Acid3 Compliance Verification & Roadmap — Version 7

**Date:** 2026-03-13
**Branch:** `copilot/verify-html-renderer-yantrajs`
**Broiler CLI version:** `net8.0`, YantraJS 1.2.295, HtmlRenderer 1.5.2 (SkiaSharp)
**Previous:** [acid3-compliance-v6.md](acid3-compliance-v6.md)

---

## 1. Current Status

### Acid3 DOM Score: **100/100** ✅

All 100 Acid3 subtests pass. Achieved via Phases A–E in v6 (see
[acid3-compliance-v6.md](acid3-compliance-v6.md) for details).

### CLI Tests: **544/544 passing** (0 failures)

### Pixel Comparison Summary

| Metric | Value |
|--------|-------|
| Total pixels compared | 786,432 |
| Exact matches | ~104,700 (13.3%) |
| Near matches (≤5) | ~108,400 (13.8%) |
| Significant differences (>25) | ~674,000 (85.7%) |

**Score is 100/100, but the rendered output is not pixel-perfect.**
The remaining 85.7% significant pixel differences are caused by
rendering engine limitations documented below.

---

## 2. Acid3 Output

### 2.1 Broiler CLI Render

```bash
dotnet run --project src/Broiler.Cli -- \
  --capture-image "file://$(pwd)/acid/acid3/acid3.html" \
  --output acid/acid3/acid3.png --width 1024 --height 768 --full-page
```

Output: `acid/acid3/acid3.png` — 1024×891 px RGBA PNG (full-page mode extends
below the 768 px viewport).

### 2.2 Chromium Reference

Reference image at `acid/acid3/acid3-reference.png` — 1024×768 px RGB PNG
(Chromium 100/100, all content within viewport).

### 2.3 Diff Image

Generated by `scripts/acid3-compare.py`:

```bash
python3 scripts/acid3-compare.py acid/acid3/acid3.png acid/acid3/acid3-reference.png \
  --output-dir acid/acid3/
```

Diff image at `acid/acid3/acid3-diff.png` — colour-coded:
- **Green:** pixel match (difference ≤ 10)
- **Red:** Broiler has darker content where reference is lighter
- **Blue:** Reference has darker content where Broiler is lighter
- **Yellow:** Other colour differences

---

## 3. Detailed Visual Difference Analysis

Despite achieving 100/100 DOM score, the rendered image differs significantly
from Chromium's output. The differences fall into the following categories:

### 3.1 Background & Container Layout

**Symptom:** Grey (`#808080`) background extends differently in Broiler vs
Chromium. The Acid3 page uses `#outer` with CSS that paints the grey border.
HtmlRenderer's default canvas background contributes additional grey area.

**Impact:** High pixel-diff count (most of the 85.7% difference) but not a
content fidelity issue — the grey is intentional in Acid3 CSS.

**Root cause:** HtmlRenderer renders the full page to a bitmap with a grey
canvas background. Chromium renders only within the viewport with correct
CSS background cascade.

### 3.2 Bucket Bar Sizing & Positioning

**Symptom:** The six coloured bucket bars (which show test progress as
red/orange/yellow/lime/blue/purple rectangles at 100/100) render with
different widths, heights, and vertical alignment.

**Root cause:**
- `display: inline-block` + `vertical-align` layout differs from CSS 2.1 spec
- Complex `em`-based sizing (e.g., `width: 2em`, `margin: -2.19em`) depends
  on correct font-size inheritance and computed value resolution
- HtmlRenderer's line-box generation for inline-block elements is imprecise

### 3.3 Score Display Formatting

**Symptom:** The score slash (`/`) renders in red instead of black. The score
text positioning differs slightly.

**Root cause:** CSS rule `#slash { color: red; color: hsla(0, 0%, 0%, 1.0); }`
uses HSLA fallback. ~~HtmlRenderer does not support `hsla()`, so the fallback
`color: red` is used.~~ **Fixed:** `CssValueParser.GetColorByHsla()` now
correctly parses `hsla()` and the second declaration overrides `color: red`.

### 3.4 Font Metrics & Text Rendering

**Symptom:** Instructions paragraph text appears concatenated
("Topassthetest,…") with compressed word spacing.

**Root cause:** `white-space: pre-wrap` is only partially supported by
HtmlRenderer. Inter-word spaces may collapse in certain contexts.
Additionally, `font: 900 small-caps 10px sans-serif` shorthand on `#linktest`
may affect nearby layout calculations.

### 3.5 Absolute Positioning

**Symptom:** Elements using `position: absolute` (score overlay, linktest
element) render at incorrect positions.

**Root cause:** HtmlRenderer's CSS positioning model has limitations with
absolute positioning inside relatively-positioned containers.

### 3.6 CSS Features Not Supported

| CSS Feature | Used By | Status |
|-------------|---------|--------|
| `hsla()` colour function | `#slash` colour | ✅ Supported — `CssValueParser.GetColorByHsla()` parses correctly |
| `white-space: pre-wrap` (full) | `#instructions:last-child` | ⚠️ Partial |
| `#id.class` compound selector | `#linktest.pending` | ✅ Supported — `DomParser` looks up `#id.class` keys |
| `display: inline-block` layout | Bucket elements | ⚠️ Partial |
| `vertical-align` with inline-block | Bucket layout | ⚠️ Partial |
| Sub-pixel layout rounding | All elements | ⚠️ Different from Chromium |
| Font anti-aliasing | All text | ⚠️ Different (SkiaSharp vs Chromium Skia) |

### 3.7 Region-by-Region Analysis

| Region | Mean Pixel Difference | Key Issues |
|--------|----------------------|------------|
| Score area (350–700, 0–80) | 110.9 | Red slash, font positioning |
| Bucket area (0–1024, 80–400) | 73.0 | Inline-block layout, vertical-align |
| Bottom area (0–1024, 400–768) | 61.6 | Background extension, text wrapping |

---

## 4. Roadmap — Version 7: Pixel-Perfect Acid3

### Overview

The v7 roadmap targets near pixel-perfect rendering of Acid3, addressing the
visual differences that remain after achieving 100/100 DOM score. The goal is
to reduce significant pixel differences from 85.7% to below 20%.

### 4.1 HTML/DOM Compliance

**Goal:** Enforce spec-correct parsing and DOM tree generation for all
Acid3-specific constructs.

| Task | Priority | Effort | File(s) |
|------|----------|--------|---------|
| Validate DOM tree output against Chromium DOM snapshot | High | 2h | `DomBridge.Registration.cs` |
| Implement remaining DOM APIs used by Acid3 (if any) | Medium | 4h | `DomBridge.JsObjects.cs` |
| Ensure `document.write()` fragment ordering matches spec | Low | 1h | `DomBridge.Registration.cs` |

**Status:** ✅ DOM compliance is complete at 100/100. No further DOM work
is needed for Acid3 score. Visual differences are in rendering, not DOM.

### 4.2 CSS and Layout Correctness

**Goal:** Match Acid3 pixel output nearly perfectly: line-box handling,
reflow, font metrics, box shadows, positioning.

| Task | Priority | Effort | File(s) |
|------|----------|--------|---------|
| ~~Implement `hsla()` colour parsing~~ | ~~High~~ | ~~3h~~ | ✅ Done — `CssValueParser.GetColorByHsla()` |
| **Fix `display: inline-block` line-box generation** | High | 8h | `HtmlRenderer/Core/Dom/CssLayoutEngine.cs` |
| **Fix `vertical-align` with inline-block** | High | 6h | `HtmlRenderer/Core/Dom/CssBox.cs` |
| **Fix `position: absolute` in relative containers** | Medium | 6h | `HtmlRenderer/Core/Dom/CssBox.cs` |
| ~~Implement `#id.class` compound selector matching~~ | ~~Medium~~ | ~~4h~~ | ✅ Done — `DomParser.CascadeApplyStyles()` + `MatchesSelectorItem()` |
| **Full `white-space: pre-wrap` support** | Medium | 4h | `HtmlRenderer/Core/Dom/CssBox.cs` → `ActWhitespace()` |
| Fix `em` unit computation with inherited font-size | Medium | 3h | `HtmlRenderer/Core/Dom/CssBox.cs` |
| Fix sub-pixel layout rounding to match Chromium | Low | 8h | `HtmlRenderer/Core/Dom/CssLayoutEngine.cs` |

**Estimated total:** ~35 hours (7h completed)

#### 4.2.1 `hsla()` Colour Parsing (Detailed)

The `#slash` element uses:
```css
#slash { color: red; color: hsla(0, 0%, 0%, 1.0); }
```

~~HtmlRenderer's `CssValueParser.GetActualColor()` does not recognise `hsla()`
syntax, so the fallback `color: red` is applied. This causes the score slash
to render in red instead of black.~~

**Status:** ✅ Complete. `CssValueParser.GetColorByHsla()` and
`CssValueParser.GetColorByHsl()` parse `hsl()`/`hsla()` colour functions
correctly, including edge cases (missing `%` signs, alpha channel). The
`HslToRgb()` conversion uses the standard CSS algorithm. The second
`color: hsla(0, 0%, 0%, 1.0)` declaration correctly overrides `color: red`
in the CSS cascade. Verified by 4+ regression tests in `CssRenderingTests.cs`.

#### 4.2.2 Compound Selector `#id.class` (Detailed)

Acid3 uses compound selectors like `#linktest.pending` and
`#bucket1.zPPPPPPPPPPPPPPPP` to conditionally style elements.

**Status:** ✅ Complete. Two fixes applied to `DomParser.cs`:
1. `CascadeApplyStyles()`: After looking up `#id`, also tries `#id.class`,
   `#id.class1.class2`, and `tag#id` compound keys in `CssData`.
2. `MatchesSelectorItem()`: Handles `#id.class` compound selectors by
   splitting on the first `.` after `#id` and verifying all class parts
   match the element's class attribute.

Verified by 4 regression tests in `Acid3RegressionTests.cs`.

The Acid3 bucket bars use `display: inline-block` with complex sizing:
```css
.bucket { display: inline-block; width: 2em; height: 1em; }
```

HtmlRenderer's layout engine does not correctly compute line boxes for
inline-block elements. The boxes are stacked differently, causing the
bucket gradient pattern to render incorrectly.

**Implementation plan:**
1. Audit `CssLayoutEngine.cs` box generation for inline-block.
2. Implement CSS 2.1 §10.6.1 inline-block height calculation.
3. Implement CSS 2.1 §10.8 line-height calculation for inline-block.
4. Fix `vertical-align` to correctly position inline-block elements.

### 4.3 Font Rendering and Graphics

**Goal:** Ensure text sizes, boldness, colour, and anti-aliasing match
Chromium as closely as possible.

| Task | Priority | Effort | File(s) |
|------|----------|--------|---------|
| Audit font metric differences (line height, ascent, descent) | High | 4h | `HtmlRenderer/Core/Dom/CssBox.cs` |
| Fix `font` shorthand property parsing | Medium | 3h | `HtmlRenderer/Core/Parse/CssValueParser.cs` |
| Investigate SkiaSharp text rendering options | Low | 2h | `HtmlRenderer.Image/` |
| SVG text rendering accuracy | Low | 4h | `DomBridge.JsObjects.cs` (SVG sub-documents) |

**Estimated total:** ~13 hours

**Note:** Font anti-aliasing differences between SkiaSharp and Chromium's
HarfBuzz/Skia pipeline are inherent and cannot be eliminated completely.
The goal is to minimise differences in text positioning and sizing.

### 4.4 JavaScript Engine (YantraJS)

**Goal:** Ensure all JS language features required by Acid3 are working.

| Task | Priority | Effort | Status |
|------|----------|--------|--------|
| `for...in` loop fix (byref DeclareLocal) | — | — | ✅ Fixed in v6 Phase B |
| `(-0).toExponential()` negative zero | — | — | ✅ Fixed in v6 Phase A |
| Unicode escape `\u002b` parse error | — | — | ✅ Fixed in v6 |
| `FunctionExpression.name` | — | — | ✅ Fixed in v6 |
| Regex `d` flag + named groups | — | — | ✅ Fixed in v6 |
| Event loop / timer accuracy | Medium | 4h | ⚠️ Static capture — not applicable |

**Status:** ✅ All Acid3-required JS features are implemented. No further
YantraJS work is needed for Acid3 compliance.

### 4.5 Animations and Timing

**Goal:** Match Acid3's animation sequence, timing, and steps.

| Task | Priority | Effort | Notes |
|------|----------|--------|-------|
| CSS animation support | Low | 16h | Not applicable to static image capture |
| Timer-based animation frame stepping | Low | 8h | Would require video/multi-frame capture |
| `requestAnimationFrame` polyfill | Low | 4h | Not triggered in static capture mode |

**Note:** Acid3 spec requires "smooth animation", but this is not testable
via static image capture. Animation support would require a live rendering
mode (e.g., the WPF application `Broiler.App` or a headless video capture).
This is out of scope for the CLI pixel test.

### 4.6 Test Infrastructure

**Goal:** Provide scripts to automate rendering, comparison, and regression
tracking.

| Task | Priority | Effort | Status |
|------|----------|--------|--------|
| `scripts/acid3-compare.py` — pixel comparison script | High | — | ✅ Created |
| `scripts/acid3-pixel-test.sh` — automation pipeline | High | — | ✅ Created |
| Chromium reference rendering (Playwright) | Medium | 1h | ✅ Integrated in pipeline |
| Regression test for comparison infrastructure | Medium | 1h | ✅ Created |
| CI workflow for automated pixel comparison | Low | 2h | Planned (no `.github/workflows/` yet) |
| Historical diff tracking (version-to-version comparison) | Low | 4h | Planned |

---

## 5. Test Infrastructure

### 5.1 Automated Pixel Test Pipeline

The full pipeline is automated via `scripts/acid3-pixel-test.sh`:

```bash
# Full pipeline (renders with Broiler CLI + Chromium, then compares)
./scripts/acid3-pixel-test.sh

# Skip Chromium reference (use existing reference image)
./scripts/acid3-pixel-test.sh --skip-reference

# Custom output directory
./scripts/acid3-pixel-test.sh --output-dir /tmp/acid3-results
```

### 5.2 Pixel Comparison Script

The comparison script `scripts/acid3-compare.py` provides:

- Per-pixel RGB difference computation
- Classification into exact/near/significant buckets
- Region-based analysis (score area, bucket area, bottom area)
- Background vs content pixel classification
- Colour-coded diff image generation
- Human-readable report output

```bash
python3 scripts/acid3-compare.py <broiler.png> <reference.png> --output-dir <dir>
```

### 5.3 Output Files

| File | Description |
|------|-------------|
| `acid/acid3/acid3.png` | Broiler CLI render of Acid3 |
| `acid/acid3/acid3-reference.png` | Chromium reference render |
| `acid/acid3/acid3-diff.png` | Colour-coded pixel diff image |
| `docs/images/acid3-broiler-v6.png` | Historical Broiler render |
| `docs/images/acid3-chromium-v6.png` | Historical Chromium reference |
| `docs/images/acid3-diff-v6.png` | Historical diff image |

---

## 6. Priority Matrix

### High Priority (Week 1–2)

1. ~~**`hsla()` colour parsing**~~ — ✅ Complete (CssValueParser.GetColorByHsla)
2. ~~**Compound selector `#id.class`**~~ — ✅ Complete (DomParser compound lookup)
3. **Inline-block layout** — Fixes bucket bar rendering (most visible structural difference)

### Medium Priority (Week 3–4)

4. **Absolute positioning** — Fixes score overlay and linktest positioning
5. **`white-space: pre-wrap`** — Fixes instructions text wrapping
6. **Font metrics audit** — Identifies and fixes text sizing differences
7. **`em` unit computation** — Fixes bucket sizing calculations

### Low Priority (Week 5+)

8. **Sub-pixel rounding** — Diminishing returns, inherent engine difference
9. **Font anti-aliasing** — Cannot fully match Chromium's rendering pipeline
10. **Animation support** — Not applicable to static image capture

---

## 7. Success Criteria

| Metric | Current | Target (v7) |
|--------|---------|-------------|
| Acid3 DOM score | 100/100 | 100/100 (maintained) |
| Exact pixel matches | 13.3% | >40% |
| Near matches (≤5) | 13.8% | >50% |
| Significant differences (>25) | 85.7% | <20% |
| Score area mean diff | 110.9 | <30 |
| Bucket area mean diff | 73.0 | <25 |
| CLI tests passing | 544/544 | 544+ |
| Red FAIL pixels | 0 | 0 |

---

## 8. References

- [Acid3 Test Page](http://acid3.acidtests.org/)
- [acid3-compliance-v6.md](acid3-compliance-v6.md) — v6 (100/100 score achieved)
- [roadmap/yantrajs-and-dom-range.md](roadmap/yantrajs-and-dom-range.md) — YantraJS & DOM Range roadmap
- [CSS 2.1 Specification](https://www.w3.org/TR/CSS21/) — Normative reference for layout
- `scripts/acid3-pixel-test.sh` — Automated test pipeline
- `scripts/acid3-compare.py` — Pixel comparison tool

---

**Status:** v7 work in progress. DOM score 100/100 maintained. CLI tests:
544/544 passing (0 failures). `hsla()` colour parsing and `#id.class` compound
selector matching verified complete. Remaining pixel-perfect rendering requires
CSS layout engine improvements (§4.2) — inline-block, vertical-align, absolute
positioning, white-space pre-wrap. Test infrastructure scripts created and verified.
