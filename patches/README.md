# Pending submodule patches

Fixes that belong in a submodule (`Broiler.HTML`, `Broiler.CSS`, `Broiler.DOM`,
`Broiler.JS`, `Broiler.Graphics`) but could not be pushed to their remote from
the session that wrote them: the git proxy only authorises repos in the session's
GitHub scope, so a push to a submodule remote outside it returns **403**. Rather
than bump a submodule pointer at a commit CI cannot clone, the change is captured
here as a `git format-patch` file for a maintainer to apply.

The directory is a backlog, not an archive: it holds only what is *currently*
pending. A patch is deleted from here, along with its row below, once its fix is
upstream and the submodule pointer is bumped.

## Applying

```sh
cd <Submodule>
git am --keep-cr ../patches/NNNN-<slug>.patch
git push origin HEAD
cd ..
git add <Submodule>        # bump the pointer only after the push succeeds
```

**`--keep-cr` is not optional for a patch that touches a file with CRLF line endings**, which
several `Broiler.JS` sources are (mixed CRLF and LF, within one file). `git am` runs the patch
through `mailinfo`, which normalizes the line endings of the diff body unless told not to — so the
context lines stop matching the file and the apply fails with *"patch does not apply"* on a patch
that is perfectly good. `git apply` does not have the problem, which is exactly why it is not the
check: these instructions use `am`, so `am` is what a patch has to survive. Verified per patch by
applying it to a clean checkout of the pinned pointer and diffing the result against the branch it
was generated from.

## Exercised on CI before they land

`scripts/apply-pending-wpt-patches.sh` applies the patches listed in its
`PENDING_PATCHES` array to the checked-out submodule trees before the WPT run, so
a pending fix is reflected in CI's numbers rather than waiting on the pointer. It
is idempotent — a patch already contained in the pinned pointer reverse-applies
and is skipped — so an entry stops applying by itself once a maintainer lands it.

## Index

| Patch | Submodule | Note |
| --- | --- | --- |
| `0102-css-dir-pseudo-class` | `Broiler.CSS` | **`:dir()` matched every element, in both directions at once.** It was listed in `CssSelectorMatcher`'s `RecognizedPseudoClasses` but had no arm in the pseudo-class switch, so it fell through to the deliberately-lenient default for recognised-but-unmodelled names — which means `:dir(ltr)` *and* `:dir(rtl)` applied to the whole document. That is how a single shadow-tree rule painted an entire canvas in issue #1538 problems 16 and 20 (`css/css-shadow/shadow-directionality-001` and `-002`, both at ~1% match). It now resolves HTML's directionality concept: the nearest ancestor-or-self with a valid `dir` attribute, `ltr` at the root, with `dir=auto` — and `<bdi>`, whose default it is — resolved from the first strong directional character, skipping `<script>`/`<style>` and any descendant that declares its own direction. An argument other than `ltr`/`rtl` matches nothing (Selectors 4 §11.2). **Strictly a narrowing**, so it can only remove matches the lenient default invented. **The two tests it is named for do not depend on it**: they are carried over the threshold by the main-repo shadow-tree scoping that shipped with it (1.1–1.3% → 99.55%, measured), and this makes them pass for the right reason rather than because every `:dir()` matched. 7 tests in `CssSelectorMatcherDirTests` cover both directions, inheritance, an invalid attribute value, `auto` in three shapes, the `<bdi>` default and the non-`ltr`/`rtl` argument. Applies cleanly to the pinned `Broiler.CSS` pointer (`076ed5d`) and survives `git am --keep-cr`. Listed in `scripts/apply-pending-wpt-patches.sh`, so the WPT run exercises it on CI ahead of a maintainer landing it. |
| `0113-html-textarea-editable-control` | `Broiler.HTML` | **`<textarea>` was not recognised as a text-entry control, so the one hit test a shell can use to start editing a field never reported it.** `HtmlContainerInt.GetEditableInputAt` is the renderer's only public answer to "is there something typeable here", and `IsEditableInputControl` matched `<input>` and the text-entry `type` values only. That is not a corner case: **google.com's search box is a `<textarea name="q">`**, so on the page the browser is most often pointed at, the field a user clicks first was the one field the renderer said was not editable. It now recognises `<textarea>` unconditionally (a textarea has no non-text mode to exclude, unlike `<input type=checkbox>`), reports its type as `textarea`, and — per HTML Forms, where a textarea's value is its **text content** and it has no `value` attribute at all — reads the value by collecting the box's text content and writes it back through `SetGeneratedTextContent` instead of setting a `value` attribute. The `<input>` path is untouched: same types, same attribute write, and the shared `SetEditableInputValue` still sets `value` for everything that is not a textarea. **Strictly an addition** — it can only make a control editable that previously was not, and no `<input>` changes classification. **The main repo does not depend on it to build or to pass**: `HtmlFormEditor` (src/Broiler.Browser.Core) hosts whatever the renderer reports, so against the pinned pointer a textarea is simply not offered and text `<input>` editing works regardless; `HtmlFormEditorTests.TextAreasAreEditableAndCarryTheirTextContentAsTheValue` asserts the full behaviour when the patch is applied and the graceful degradation — nothing claims a control that was not offered — when it is not, so the suite is green either way and turns into a real assertion the moment a maintainer lands this. Note the paired **main-repo** fix it needs to be visible: `CssBox.SetGeneratedTextContent` now re-splits the box into words, without which an edited control's new value had no words and painted blank. **Not listed in `scripts/apply-pending-wpt-patches.sh`**: it changes what is *interactive*, not what is rendered, so no WPT test moves. Applies cleanly to the pinned `Broiler.HTML` pointer (`32da8db`) and survives `git am --keep-cr` (the file is CRLF). |
| `0114-graphics-font-cache-thread-safety` | `Broiler.Graphics` | **The fallback font's glyph caches were plain `Dictionary` instances on a process-wide singleton, written with no synchronization.** `FallbackSystemFont` is reached through `LazyShared`, and text measurement is not confined to one thread, so two threads measuring at once tore the buckets and threw `IndexOutOfRangeException` from inside `Dictionary.TryInsert` — a stack trace that names the font code rather than whatever was measuring. Fixed by making the four caches `ConcurrentDictionary`, which keeps the hot read path lock-free. **Found while adding modifier state to pointer events, and unrelated to it**: it surfaced as `Broiler.UI`'s toolbar tests failing intermittently, since xunit runs an assembly's test classes in parallel and several of them measure button text. **Measured before it was attributed**: the same suite flakes on a clean tree (1 of 6 runs) as well as on the branch (2 of 6), so it predates the work it was found under; with the fix it passed 8 of 8. **The regression test is checked both ways rather than merely written** — it fails 5 of 5 runs against the plain-`Dictionary` version and passes 5 of 5 with the fix. That took a second attempt worth recording: the obvious `Parallel.For` version passed *even with the bug present*, because the singleton gives a process exactly one cold window in which inserts race, and by the time the loop had spread out the cache was already warm. The committed test lines threads up on a `Barrier` and has them insert distinct interleaved keys so they collide inside that one window. **Not listed in `scripts/apply-pending-wpt-patches.sh`**: it changes thread safety, not what is rendered, so no WPT test moves. Applies cleanly to the pinned `Broiler.Graphics` pointer (`4df260b`) and survives `git am --keep-cr`. |
| `0115-js-per-pattern-regex-compilation-race` | `Broiler.JS` | **Phase 5's item 2: decide `RegexOptions.Compiled` per pattern by racing both forms on the real subject, plus the three probes that measure it** (`docs/performance-roadmap.md` phase 5). The item had been left deliberately unshipped because one of seven real Octane patterns — an ordinary `trim` — measured **4.3× slower compiled**, stable across three repetitions, which kills "compile after N uses": a trim is exactly the pattern a program runs hundreds of thousands of times. The section closed by naming the design most likely to survive a wider corpus — *measure both forms once on the real subject and keep the winner, because that needs no predicate at all* — and this is it. A `JSRegExp` counts its own matches; at the thousandth the engine builds the compiled form, times both arms ABBA-interleaved on the subject in hand under a 4 ms budget, and adopts the compiled one only if it wins by 1.15×; verdicts are cached by (pattern, options) so sibling instances of a literal inherit rather than race again. **The arm the race picks is unobservable** — `RegexOptions.Compiled` changes code generation and nothing else — and `RegexTieringTests` asserts that directly: **15 cases run on BOTH settings and required to agree**, covering captures and their order, named groups, `lastIndex` across a global exec loop, sticky in both directions, a backreference, `replace` with a function, `split`, the Annex B statics, a gap-feature pattern routed to `Broiler.Regex` that must never race, and `RegExp.prototype.compile` replacing a matcher that has already gone hot. All 15 pass on the unmodified engine too. **Off by default (`BROILER_JS_REGEX_TIERING=1`)**, because measuring it found no speed-up worth a retained `DynamicMethod` per hot pattern: **1.010× on 3 of 6 interleaved pairs** of Octane's RegExp suite, the only one of fifteen it reaches — eleven build no regex at all. **Two findings came out of measuring it and both are in the roadmap**: the 4.3× regression the original decision rested on **does not reproduce** (re-run unchanged, all three losing rows change sign, and the shape in question promotes at 5.27× on Octane's own subject), and `--regex-call-envelope` says why nothing aimed at matching can move this suite — **the matcher is 4.6–6.5% of what `re.test` costs and 8.7–9.4% of what `re.exec` allocates**, with a fixed ~2.4 µs and 2 431 B per call that does not change when the subject grows 18.8×. **Strictly additive**: one `int` field on `JSRegExp`, one compare in `RunMatch` that is never taken while the switch is off, and three new files. **The main repo does not depend on it** — it is a performance switch with no observable behaviour, so nothing in `src/` changes either way. Repository suite **8 219 tests across 13 projects, 0 failures**; test262 unchanged over all five pinned manifests (§3.4). Applies to the pinned `Broiler.JS` pointer (`e5dc2610`) and survives `git am --keep-cr`. |
| `0116-html-svg-viewbox-intrinsic-sizing` | `Broiler.HTML` | **An outer `<svg>` was sized to the 300×150 default object size, whatever its `viewBox` said.** In HTML an outer `<svg>` is a replaced element whose `width`/`height` presentation attributes default to `auto` and whose `viewBox` supplies an intrinsic aspect ratio but no intrinsic size (SVG 2 §8.2); CSS then resolves the auto width to 100% of the containing block and transfers it into the auto height through that ratio, so `<svg viewBox="0 0 500 500">` in the body is a viewport-wide square. Broiler gave every such element 300×150, so a drawing the reference browser paints across the whole page came out as a 150px letterboxed square — **5.4% pixel match on four of issue #1552's top 30** (`inert/inert-svg-hittest`, `the-dialog-element/inert-svg-hittest`, `accessibility/svg-mouse-listener`, `svg/animations/svgrect-animation-invalid-value-1`), three of which now pass at 100% and the fourth of which reaches 97.0% (its residual is dialog text centring, unrelated). The auto height is left for the layout engine to transfer, because a 100% width is not resolvable at cascade time; the reverse — an auto width from a definite pixel height — is computed in the patch. With no usable ratio (no `viewBox`, a malformed one, or a non-positive extent) each auto axis independently keeps the 300×150 default, so `<svg width="100">` stays 100×150. Also stops `TranslateLength` turning an `auto` dimension attribute into the unparseable `"autopx"`, which collapsed `<svg width="auto">` to a zero-width box that painted nothing. **Needs the main-repo half to have any effect** — the patch only supplies the ratio, and the transfer that consumes it is `CanTransferAspectRatioToBlockHeight` in `Broiler.Layout`, already in-tree. Sizing was calibrated against Chromium on 24 constructed cases (23 match; the 24th is a degenerate `viewBox="0 0 0 0"`, which Chromium sizes the same way but declines to *render* — a separate painting rule). 22 cases in `SvgReplacedSizingTests`. Measured before/after on three WPT subsets: 965 css tests byte-identical, 184 dialog tests 0 regressions, 384 svg/inert/accessibility tests +4 passes and average match 97.97% → 98.74% (2 regressions, both tests where Broiler paints an `feConvolveMatrix`-filtered rect it cannot filter — a pre-existing gap the correct, larger box makes more visible). Listed in `scripts/apply-pending-wpt-patches.sh`, so the WPT run exercises it on CI ahead of a maintainer landing it. Applies cleanly to the pinned `Broiler.HTML` pointer (`32da8db`) and survives `git am --keep-cr`. |
| `0117-css-constraint-validation-pseudo-classes` | `Broiler.CSS` | **`:valid`, `:invalid`, `:required` and `:optional` matched every element.** All four were listed in `CssSelectorMatcher`'s `RecognizedPseudoClasses` with no arm in the pseudo-class switch, so they fell through to the deliberately-lenient recognised-but-unmodelled default — the same shape as `0102`'s `:dir()`. That includes `<html>` and `<body>`, whose background propagates to the canvas, so a bare `:invalid { background-color: … }` — how WPT's form-validation tests write it, with no tag qualifier — **painted the whole page instead of the failing control**: issue #1552 problem 21, `html/semantics/forms/constraints/form-validation-validity-textarea-defaultValue`, rendered a fully pink canvas against a reference of four pink boxes on white, **3.78% → 97.04%** (its residual is the default `<textarea>` width, 176px against the reference's 182px — a separate font-metric question). They now resolve HTML §4.10.16: `:valid`/`:invalid` match a `<form>`/`<fieldset>` (state taken from the controls they contain) or a control that is a *candidate for constraint validation*, so an element barred from it — disabled, readonly, inside a `<fieldset disabled>`, or a constraint-free input type — matches **neither** rather than defaulting to valid. Violations modelled: the missing `required` value (text content for `<textarea>`, checked state for a checkbox, selected option for a `<select>`), the `email`/`url` type mismatches, the `pattern` mismatch (anchored both ends) and the numeric `min`/`max` range. **`minlength`/`maxlength` are deliberately never consulted** — HTML makes "suffering from being too short/long" conditional on the value having been *edited by the user*, and nothing in a static render has been, which is exactly what the test above pins when it requires `<textarea minlength=5 required>a</textarea>` to be valid. A lone unchecked required radio is left valid, matching the reference browser, because that state belongs to the radio group. **Strictly a narrowing**, so it can only remove matches the lenient default invented. Calibrated against the reference browser on 40 constructed cases spanning every element type, barred state, input type and violation — Broiler reproduces all 40 exactly — and 40 cases in `CssSelectorMatcherValidityTests` cover the same ground. Regression runs with no pass/fail movement: 965 css tests and 184 the-dialog-element tests byte-identical, 44 forms-constraints tests unchanged but for the test named above. Listed in `scripts/apply-pending-wpt-patches.sh`, so the WPT run exercises it on CI ahead of a maintainer landing it. Applies cleanly to the pinned `Broiler.CSS` pointer (`f960f94`) and survives `git am --keep-cr`. |
| `0118-dom-attribute-character-references` | `Broiler.DOM` | **An attribute value's character references were never decoded, so the DOM held the source text that spelled the value rather than the value.** WHATWG §13.2.5 decodes references in an attribute value in the tokenizer, exactly as in character data; this tokenizer left the raw text and let `Broiler.HTML`'s box builder decode it on the way to the renderer. The rendering was therefore right and every reader of the DOM wrong by exactly one level of escaping: `getAttribute("href")` on `href="?a=1&amp;b=2"` returned `?a=1&amp;b=2`, an `[attr="…"]` selector had to be written against the escaped spelling, and serializing escaped it again — so a DOM round-trip grew another `&amp;` each time round. Only a reference terminated by `;` is decoded, which is what makes the shared `DecodeReferences` safe for attributes: the spec's ambiguous-ampersand rule exists to stop `href="?a=1&copy=2"` becoming a `©`, and a semicolon-only decoder never had to be told; the cost is the other half of that rule (a terminator-less `title="&copy"` stays literal), the same conservative gap the helper already had in character data. **Must be applied together with `0119`, and before it** — they are halves of one balance, and 0118 alone makes the renderer decode twice. 8 tests in `HtmlTokenizerAttributeReferenceTests` cover named/decimal/hex references, the single-level-of-escaping case, the unterminated reference, an unquoted value and a reference-free value; 8 more in the main repo's `HtmlEntityDecodingTests` cover the bridge DOM, an attribute selector, the serialize round trip and the render. **Not WPT-visible on its own**: `css/css-view-transitions` (490), `html/semantics/interactive-elements` (184), a 965-test css sweep and `inert;accessibility;svg/animations` (384) came out **byte-identical** across the pair — same passes, same average match — which is the point, the renderer sees the same values while the DOM stops being wrong. Applies cleanly to the pinned `Broiler.DOM` pointer (`358cf05`). Listed in `scripts/apply-pending-wpt-patches.sh`. |
| `0119-html-attribute-value-taken-as-is` | `Broiler.HTML` | **The other half of `0118`.** The box builder HTML-decoded every attribute value on its way into `HtmlTag`, compensating for the undecoded tokenizer. With `0118` decoding them where every other consumer of the DOM sees them, decoding again here would eat a second level of escaping — `title="&amp;amp;"` means a literal `&amp;`, not `&`. **Must be applied together with `0118`, and after it**: this patch alone would leave every attribute value escaped everywhere, including in the renderer. Applies cleanly to the pinned `Broiler.HTML` pointer (`b829d1f`). Listed in `scripts/apply-pending-wpt-patches.sh`. |
