# Pending submodule patches

Fixes that belong in a submodule (`Broiler.HTML`, `Broiler.CSS`, `Broiler.DOM`,
`Broiler.JS`, `Broiler.Graphics`) but could not be pushed to their remote from
the session that wrote them: the git proxy only authorises repos in the session's
GitHub scope, so a push to a submodule remote outside it returns **403**. Rather
than bump a submodule pointer at a commit CI cannot clone, the change is captured
here as a `git format-patch` file for a maintainer to apply.

## Applying

```sh
cd <Submodule>
git am --keep-cr ../patches/NNNN-<slug>.patch
git push origin HEAD
cd ..
git add <Submodule>        # bump the pointer only after the push succeeds
```

Delete the patch file and its row below once the pointer is bumped.

**`--keep-cr` is not optional for a patch that touches a file with CRLF line endings**, which
several `Broiler.JS` sources are (mixed CRLF and LF, within one file). `git am` runs the patch
through `mailinfo`, which normalizes the line endings of the diff body unless told not to — so the
context lines stop matching the file and the apply fails with *"patch does not apply"* on a patch
that is perfectly good. `git apply` does not have the problem, which is exactly why it is not the
check: these instructions use `am`, so `am` is what a patch has to survive. Verified per patch by
applying it to a clean checkout of the pinned pointer and diffing the result against the branch it
was generated from.

## Index

| Patch | Submodule | Note |
| --- | --- | --- |
| `0088-css-nth-child-of-selector` | `Broiler.CSS` | **A missing `-1` check, and the blast radius is the whole page.** `:nth-child(An+B of S)` looked the element's position up in the *filtered* sibling list without checking the lookup succeeded. An element that does not match `S` gets index `-1`, and the count-from-the-end branch computed `siblings.Count - (-1)` — a positive position the An+B test accepts — so `:nth-last-child(odd of webkit, fast)` matched every non-matching element whose filtered sibling count is even. **`<html>` is one of them**, and a root background propagates to the canvas, so the reftest's `background-color: lime` painted the entire viewport rather than two elements. The forward branch already rejected the same case through its `position > 0` test, so only the from-the-end direction changes behaviour. **This patch has no main-repo fallback** — selector matching lives entirely in `Broiler.CSS.Dom.CssSelectorMatcher`, with no parent-repo layer to shim it at — so WPT `css/selectors/nth-last-child-of-tagname` stays red until it is applied. Measured against a locally generated Chromium reference at the runner's own metric (±5 per channel): **0.09% → 98.28%**. It does not cross the 99% pass threshold, and the reason is *not* this fix — the test's own reference file, which contains no `:nth-child` at all, differs from its golden by the **identical 13 501 pixels**, all of it text antialiasing. The selector behaviour is exact; the residual is the engine-wide text-rasterization delta. Ships with a regression fixture (`CssSelectorMatcherTests.Nth_Child_Of_Selector_Requires_The_Element_To_Match_The_Filter`) built on the reftest's own DOM, asserting both directions and that `<html>`/`<body>` are excluded. **Independent of every other pending patch** — a different submodule from `0082`–`0086` and from `0087`. |
| `0087-html-backdrop-painting-props` | `Broiler.HTML` | **Four property names, and the reason they were missing is the interesting half.** A native `::backdrop` box is built from the bridge-resolved background plus an overlay of the author's *geometry* declarations, so every other painted property in the `::backdrop` cascade was dropped on the floor — `opacity: 0.5` on a green scrim filled the viewport with opaque `rgb(0,128,0)` instead of compositing to `rgb(127,191,127)` over the white canvas. This overlays `opacity`/`mix-blend-mode`/`border-radius`/`box-shadow` alongside the geometry pass. Deliberately narrow: these are the properties whose effect on the generated box is the same as on a real `::backdrop`, and inherited or layout-affecting ones stay out. **This patch is not what makes CI green** — the WPT runner keeps `NativeBackdrop` off, so the path it measures is the bridge's synthesized `<div>`, and the equivalent fix there (`DomBridge.BackdropPaintingProps`, `OverlayBackdropAuthorPainting`) is in the main repo and active now. This is the native path catching up, so the two do not drift; both were measured at **2.2% → 99.5%** pixel match on WPT `the-dialog-element/modal-dialog-backdrop-opacity` against a locally generated Chromium reference, the native arm via `BROILER_WPT_NATIVE_BACKDROP=1`. **Independent of `0082`–`0086`** — a different submodule entirely — so it carries no ordering constraint against them. |
| `0086-js-tonumeric-reuse` | `Broiler.JS` | **Built straight off `0085`'s census, and nine lines.** `ToNumeric` coerces the operand of `++`/`--` and hands back the coerced old value, and it minted unconditionally — so `n++` on a Number copied the Number into a **second, equal `JSNumber`**, which `0085` priced at 30.9% of the corpus's boxing being `++`/`--` and exactly half of that being the copy. **Reuse is sound because a JavaScript Number has no observable identity**: it compares by value, it cannot carry a property, and `Object.is` on two Numbers is a value comparison — the argument the small-integer cache has rested on since P2-2, where unrelated call sites are already handed the same instance. The guard is `primitive.IsNumber`, **not** `!primitive.IsBigInt`: a String, a Boolean, `null` and `undefined` all reach that line and all still have to be coerced, which is the whole reason `ToNumeric` exists (`"1"++` yields the Number 1, not the String). **Measured**, one build, `BROILER_JS_NUMERIC_UPDATE_REUSE` the only difference: **17 285 913 requests removed, 15.4%, against a census prediction of 17 281 232 — the thing built is the thing measured to 0.03%.** In allocations it is **7 050 834, 9.4%**, and *the gap between those two numbers is the small-integer cache*: Crypto removes 7.2 M requests and **5 117 boxes**, its updates being loop counters inside `[-128, 1024]` where P2-2 already answered them for free, while NavierStokes' indices run past that bound so **71.4% of its removed requests were real — 6.76 M boxes, 23.0% of everything it allocates**. EarleyBoyer **halves**. Together with `0084` the corpus goes **85 255 034 → 67 798 222 boxes, 0.795×**. Five coercions still mint on the reuse arm, which is the guard discriminating rather than a leak. **Wall clock, ABBA-interleaved, six pairs, counters off: NavierStokes 0.906× on six of six** (0.862–0.928), controls holding (RayTrace removes nothing, 0.997×), and **the driver total flat at 1.013×** — which the arithmetic predicts, since NavierStokes is 8.7% of the driver and 9.4% of it is 0.82% of the total. Richards' 1.121× is **not callable in either direction**: an 11.2% own-arm spread against a 12.1% effect, and believing it would price 3 891 boxes at 18 µs each. ***The sharpest thing in the run is what did not move***: EarleyBoyer cut **50.0%** of its boxes — the largest proportional cut — and read 1.002×, because that is 82 000 boxes a second against NavierStokes' **4 240 000**. A share of a suite's own allocation forecasts nothing; the rate forecasts everything, which is now a standing lesson in §3.5. `NumericUpdateReuseTests` — 9 fixtures, **each on both settings of the switch** — postfix and prefix, the non-Number operands that must still coerce, NaN and the infinities, `-0` through both `1/x` and `Object.is`, a `valueOf` run exactly once, a getter read once with the setter seeing the increment, BigInt, the `Symbol` TypeError, an element update, **the identity argument asserted rather than assumed**, and a counter invariant, because "the box count went down" would otherwise be consistent with the coercion having stopped happening. **This patch also rewrites one of `0085`'s own fixtures, and that is the point rather than an inconvenience**: `AnUpdateOnAPropertyCostsTwoBoxesNotOne` asserted the two-box cost this change removes, so it **failed in all three of the first suite runs** — a census fixture catching its own item's follow-up. It is now a Theory on both settings, asserting that the **coercion count stays 1 either way** and only which side of the split it lands on moves (`UnaryToNumeric` 1→0, `UnaryToNumericReused` 0→1), which is what separates *"the box went away"* from *"the coercion stopped happening"*. The census helper now pins the switch for every fixture in that file rather than inheriting it, since both the counters and the switch are process-wide statics. **Full repository suite 7 988 tests, 0 failures, on three consecutive runs.** One caveat recorded rather than rounded off: `Broiler.JavaScript.Integration.Tests` **stalled once** on an earlier build of this stack — its host measured at *one jiffy of CPU over eight seconds*, a hang and not slow progress — and **did not recur in six subsequent full-suite runs** (three of them under `--blame-hang --blame-hang-timeout 300s`, none producing a sequence file) nor when that assembly was run alone, where it passed 4 571 in 47 s. It was not resource exhaustion (27 GB disk, 13.7 GB RAM free, no stray processes). **Unexplained, not reproduced, and not attributed to this change** — if it returns, `BROILER_JS_NUMERIC_UPDATE_REUSE=0` restores the previous behaviour exactly and is the bisection. **Apply after `0085`**: it edits the same diagnostics class, the same census fixture and the same emitter. |
| `0085-js-boxing-source-census` | `Broiler.JS` | **Where the boxes `0084` did not remove are minted — all of them.** The guarded tree left **74 835 575 boxes from 111 997 550 factory requests**, and the reading that suggests itself is that they are *root* boxes on their way into a `JSValue` slot or element — which is exactly what a typed backing store, item 3-1 as originally written, would remove. Giving the compiler's boxing conversion its own factory entry makes that refutable: `JSNumber.CreateConversion`, counted apart from `Create` and `CreateLiteral` exactly as the literal entry already is, with `JSNumberBuilder.New` pointing at it. **It is 18.4% of the corpus's requests — and 5.0% of NavierStokes' against 31.0% of Crypto's**, i.e. the two suites are the opposite way round from the item's premise, and Crypto is the one the guarded tree already served best. That column is in fact the **ceiling on what a typed store can remove without further operator work**, because where the tree already computes natively the root is counted there. **The first pass stopped at that and left 40.5% of the corpus's requests attributed to nothing at all** — which by §3.5 is a claim about the census, not about the engine. Two counters take it to **1.0%**. `JSValue.BitwiseXor` turned out to be the one generic binary operator `0083` never hooked (a real gap, though the corpus says a small one, below the run-to-run spread); the rest is **the unary operators, which no census had looked at** — `-x` and `~x`, the `++`/`--` step, and the `ToNumeric` that coerces the operand of `++`/`--`. Named in the arm that is *built*, the guarded tree on: **binary operators 53 351 878 (47.6%), `++`/`--` 34 562 464 (30.9%), compiler conversion 20 601 685 (18.4%), numeric literal 1 671 314 (1.5%), unary `-`/`~` 702 031 (0.6%), unnamed 1 108 178 (1.0%)**. **The largest single source on the corpus's biggest boxer is `++`**: NavierStokes **51.6%**, EarleyBoyer **80.4%**, the corpus 30.9% — more than the compiler conversion and the numeric literal together — and **exactly half of it is waste**, because `ToNumeric` ends `primitive.IsBigInt ? primitive : CreateNumber(primitive.DoubleValue)` and so copies a `JSNumber` into a second, equal `JSNumber` to hand back as the old value, which a Number's lack of observable identity makes undetectable. **17 281 232 requests, 15.4% of the corpus's boxing, for a value the engine is already holding** — the next build, and a guard rather than a mechanism. Four new fixtures in `ArithmeticOperandCensusTests` (9 total), of which **the three that assert on the unary columns are deliberately loop-free — and the reason is the finding in miniature**: written with the `for (var i = 0; i < 10; i++)` the rest of the file uses, all three failed, because the loop counter's own `i++` lands in the columns they assert on. Counters off by default, no behaviour change, full repository suite **7 967 tests, 0 failures**. **Apply after `0083` and `0084`**: it edits the same census fixture, the same diagnostics class and the same emitter. |
| `0084-js-guarded-numeric-tree` | `Broiler.JS` | **Item 3-1's shared half, built.** `0083`'s census said the generic arithmetic operators are handed two Numbers on **73 817 515 of 73 818 646 invocations — 86.6% of every box the corpus allocates** — while the compiler's own proof reaches **0.75%**. So the shared half is not a typed backing store: the operator already receives two Numbers whatever they are stored in, and what it cannot do is *hand one back*. This is a **run-time-guarded specialization of an arithmetic expression tree** — evaluate each leaf once into a temporary, test the leaves for Number, compute the whole tree on raw doubles, box only the root. **Evaluation order is the whole correctness argument**: the ordinary emission coerces a node's operands between two leaf evaluations, and a coercion is observable, so a tree is eligible only when every leaf evaluated after the first internal node in postorder is a numeric literal or a proven-numeric local. Leaves *before* the first coercion are unrestricted, which is not a corner — precedence makes `s + a[0] * 1.5` right-leaning. `(a[0] * 2) + p.v` is refused. **Measured on the corpus**, one build, the switch the only difference: **10 401 782 boxes removed of 85 249 783 — 12.2% — from 862 compiled sites**, with generic invocations 73 823 667 → 53 354 211 (0.723×). Crypto **0.786× boxes / 0.583× invocations**, Box2D 0.933×, Richards 0.787×. Items 3-0, 3-3, 3-5, 3-7 and 3-1's bitwise half moved **0.36% between them**. **It is well short of the 86.6% ceiling and the per-suite column says why**: NavierStokes loses 10.1% of its invocations and **1.8%** of its boxes, EarleyBoyer **99.7%** of its invocations and **none** of its boxes — so most of those two suites' boxes are minted somewhere that is not a binary arithmetic operator, which is the next count to take. **One mistake caught by measuring**: the first eligibility rule required two operators, on the argument that a single node mints one box either way — right about the result, wrong about the operand, because `a[0] * 2` costs two boxes today (only `+` has a `JSValue × double` overload). That rule took the corpus from 10.4 M removed to **5.6 M**, Crypto alone losing 4.7 M; what ships counts `(operators − 1) + native leaves ≥ 1`. **Wall clock measured too**, ABBA-interleaved at process granularity, six pairs, counters off for the timing pass (they had been enabled around the driver unconditionally, which would have charged the slower arm for 20.5 M interlocked increments — a bias pointing the same way as the result): **driver total 0.981× on six of six pairs**, **Crypto 0.912× on six of six** (0.857–0.961), against the corpus's own control — DeltaBlue and EarleyBoyer remove zero boxes between the arms and read **1.005× and 1.006×**, their ~11% pair spread being the noise floor, so no per-suite effect under ~5% is callable. **No suite is slower.** *12.2% of the corpus's allocation buys 1.9% of its execution time*, which bounds the rest of phase 3. `NumericSpeculationTests` (33 cases, **each on both settings of `BROILER_JS_NUMERIC_SPECULATION`** — values, types, and order: a getter read once and left to right, a `valueOf` that mutates a later leaf, a getter that mutates a later leaf, a throwing leaf that must stop the next being read — plus two counter assertions, because all 33 also pass when the specialization never fires). Full repository suite **7 963 tests, 0 failures**. **Apply after `0083`**: it edits the same census fixture and the same emitter. |
| `0083-js-arithmetic-operand-census` | `Broiler.JS` | Item 3-1's first measurement, and it re-specifies the item off storage. Phase 3's ordering rests on the diagnosis that the boxes are minted by the **operators**; the half nobody had counted is how often a generic operator's two operands are *already* Numbers, which is what decides whether any fast path can be fed. `ArithmeticOperandDiagnostics` counts it on each generic arithmetic and bitwise operator (plus the `JSNumber` and `JSString` overrides, which a Number or String receiver reaches instead of the base): **73 817 515 of 73 818 646 invocations across the corpus arrive with both operands already Numbers — every one but 1 131 — and that population is 86.6% of all 85 248 348 boxes**. The guard a speculating native form needs is a branch that predicts perfectly. **Against it, the compiler's own `both are native` proof reaches 556 053, 0.75%** — and even that counts `+` alone, the only operator with a `JSValue × double` overload. *Compile-time provability reaches 0.75% of the arithmetic; run-time truth reaches 100.00% of it*, and six correct, invisible phase-3 items sit in that gap. **The consequence corrects item 3-1's own re-specification**: "storage plus an unboxed element read" assumes the problem is where the value is kept, and it is not — the operator already receives two Numbers whatever they are stored in; what it cannot do is *hand one back*, because its consumer is a `JSValue`. The shared half is a **run-time-guarded specialization of an arithmetic expression tree**, boxing only the root. It also partly reverses **3-8's "do not start as written"** without contradicting it: 3-8 priced that guard at the **local** (0.36% of boxing, correct), this counts it at the **operator** (86.6%). One smaller correction: a numeric literal is **already** a native double, so the second box in `a[0] * 1.5` is the compiler re-boxing a raw double to meet a `JSValue` operator, not `VisitLiteral`. **The counter's first version read zero on all seven suites** — the enable went next to the wrong one of two identical `NumberBoxingDiagnostics.Reset()` pairs — which is what `ArithmeticOperandCensusTests` exists to stop: 5 fixtures, 3 of which make the counter *discriminate* rather than merely fire. Counters off by default, no behaviour change, full repository suite **7 930 tests, 0 failures**. **Independent of `0082`** — no file in common — but generated against the same pin, so apply in number order. |
| `0082-js-relay-rewrite-once` | `Broiler.JS` | Item 1-1's remaining half — **measured before being built, and the measurement found a repeat inside the half that already landed.** Two new instruments. **`--compile-phases`** takes the parse / expression-tree / emission split on the *real* corpora rather than on `--compile-scaling`'s declaration walls, against the body-free control and with the closure rewrite as its own column: **parse 9.4–13.5%, tree construction 33.6–63.9%, emission 25–57%**, tree construction the largest single phase on five of six, and the parse — the part an early-error rule forbids deferring — a tenth of the whole. **`--defer-population`** counts what the ceiling table never could, by reading item 1-1's own registration and forcing counters after evaluating each corpus the way its harness does: **84–99.7% of a script's functions are never invoked** (jQuery 347 of 415, Mandreel 2 689 of 2 697). So the remaining half is over half the compile across a population that is almost entirely never needed — **and it also corrects this item's own ceiling table**, because `--compile-profile` stubs *outermost* bodies and jQuery has exactly one, the IIFE the library is written inside (99.91% of its bytes), which CodeLoad calls first. The build: `LambdaRewriter.Rewrite` descends through nested lambdas — that is how `CheckForClosure` threads a capture up the whole chain — and `RuntimeMethodBuilder.Relay` called it **again** per relayed site, so a lambda at depth *d* was walked *d+1* times and jQuery's whole tree was walked twice by a compile that emits almost nothing. Counted, every relay in a real compile is a repeat (**0 rewrites needed against 415, 978 and 1 574 skips**) and the repeat creates **0 captures the first walk had not** — the second counter, which is what makes the skip a removal of *repeated work* rather than of work. `Relay` now skips a lambda a descending walk has already entered, marked on the lambda; `RewriteRootOnly` — the async pre-rewrite, which stops at each nested lambda by design — marks nothing, so async and generator state machines are rewritten at relay exactly as before. Worth **0.782× on jQuery's whole compile and 0.867× on Typescript's, six of six ABBA pairs each**; **Box2D does not separate on that instrument** and its control arm's own spread is 55.6%, so the changed phase was measured directly — **emission 99.9 → 54.8 ms, 0.549×, and whole compile 267.4 → 207.2 ms, 0.775×**, in the round where `--compile-phases`' built-in parse control held (the round where it moved 1.5–1.9× is discarded on that basis). `RelayRewriteTests` (19 cases: transitive capture through three levels, a write back through one, per-instance loop cells two levels down, a generator and an async body nested in a closure, `this` through an arrow, a named function expression's own name, direct `eval` two levels down, five levels reading and writing every level — **each on both settings of `BROILER_JS_RELAY_REWRITE_ONCE`** — plus the counter invariant). **All five pinned test262 manifests were run against it** at `07adeb44` plus this patch, on linux-x64: **8 710 executed, 8 617 passed, 84 failed, 251 skipped, 9 timed out — identical to the roadmap's §3.4 row manifest by manifest**, and the same *files* rather than the same totals (all 84 need `$262`; the 9 timeouts are lines 7–15 of `test262-failures.txt`). The suite came from a `git fetch --depth 1` of the pinned ref passed through `--suite-root`, because `codeload.github.com` and `api.github.com` both 403 through this session's proxy; the runner's own "Selected 3 160 runnable test(s)" is what says it is the same corpus. No behaviour change and no main-repo fallback needed: the switch defaults on, the probes are emitters nothing else calls, and the counters are touched once per site. |

**`0082`, `0083`, `0084`, `0085` and `0086` are pending against the pinned pointer `07adeb44`.**

**`0087` is pending against the `Broiler.HTML` pinned pointer `29bf9c33`** — a different submodule from `0082`–`0086`, so the two stacks are independent. It applies cleanly to a clean checkout of that pin (`git apply --check`).

**`0088` is pending against the `Broiler.CSS` pinned pointer `dba36efb`**, likewise independent of the other two stacks and likewise clean under `git apply --check`. Of the three pending stacks it is **the one with no main-repo fallback**: `0087`'s behaviour is already live on the path CI measures, but `0088`'s is not reachable from the parent repo at all.
All five were applied from a clean checkout of that commit with **`git am --keep-cr`** — `0083`,
`0084`, `0085` and `0086` in sequence, since each edits files the ones before it add — and the
resulting tree diffed against the branch each was generated from, identical. `0082` touches no file
the other four do; **`0083` → `0084` → `0085` → `0086` is the required order**.

The push to `Broiler-Platform/Broiler.JS` returned **403** from the session's git proxy, so the
pointer is deliberately *not* bumped.

**The four patches this file carried before it (`0078`–`0081`) have all been applied, pushed and
the pointer bumped to `07adeb44`** — matched patch by patch to the submodule log rather than
inferred from this prose. They are `37905aeb` (item 3-7), `14ac195f` (3-8), `cb2e63c6` (3-1) and
`07adeb44` (3-2) in patch order, and the pointer they were pending against, `61c8cc65`, is an
ancestor of the current pin.

The thirty-three patches this file carried before them (`0049`–`0077`, and `0001`–`0048` before
those) have all been applied, pushed and their pointers bumped.
