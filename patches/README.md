# Pending submodule patches

Fixes that belong in a submodule (`Broiler.HTML`, `Broiler.CSS`, `Broiler.DOM`,
`Broiler.JS`, `Broiler.Graphics`) but could not be pushed to their remote from
the session that wrote them: the git proxy only authorises repos in the session's
GitHub scope, so a push to a submodule remote outside it returns **403**. Rather
than bump a submodule pointer at a commit CI cannot clone, the change is captured
here as a `git format-patch` file for a maintainer to apply.

The directory is a backlog, not an archive: it holds only what is *currently*
pending. A patch is deleted from here, along with its row below, once its fix is
upstream and the submodule pointer is bumped.

## Applying

```sh
cd <Submodule>
git am --keep-cr ../patches/NNNN-<slug>.patch
git push origin HEAD
cd ..
git add <Submodule>        # bump the pointer only after the push succeeds
```

**`--keep-cr` is not optional for a patch that touches a file with CRLF line endings**, which
several `Broiler.JS` sources are (mixed CRLF and LF, within one file). `git am` runs the patch
through `mailinfo`, which normalizes the line endings of the diff body unless told not to — so the
context lines stop matching the file and the apply fails with *"patch does not apply"* on a patch
that is perfectly good. `git apply` does not have the problem, which is exactly why it is not the
check: these instructions use `am`, so `am` is what a patch has to survive. Verified per patch by
applying it to a clean checkout of the pinned pointer and diffing the result against the branch it
was generated from.

## Exercised on CI before they land

`scripts/apply-pending-wpt-patches.sh` applies the patches listed in its
`PENDING_PATCHES` array to the checked-out submodule trees before the WPT run, so
a pending fix is reflected in CI's numbers rather than waiting on the pointer. It
is idempotent — a patch already contained in the pinned pointer reverse-applies
and is skipped — so an entry stops applying by itself once a maintainer lands it.

## Index

| Patch | Submodule | What it is |
|---|---|---|
| `0137-html-paged-render-surface` | `Broiler.HTML` | **A paged render paints its whole surface instead of its first page, and `HtmlContainer` exposes the page box.** 21 lines, and both halves are invisible to every current caller. `GetPaintViewport` bounded painting to `min(MaxSize, PageSize)`; `PageSize` defaults to the very large "not paginated" extent, so that minimum *was* `MaxSize` and nothing moves. It matters only for a host that paginates, where the two sizes have to mean different things: a paged render lays the continuous flow out on a surface several pages tall and cuts pages from it, so the **surface** is what gets painted while the **page** is what `vw`/`vh` and the fragmentation boundaries resolve against. Bounding the paint to the page painted page one and silently dropped the rest — the blank-page bug. **Measured on the case that found it**, `css/CSS2/pagination/block-page-break-inside-avoid-print-ref` (a 5in×3in sheet with half-inch margins, so a 4×2in page area; one block, a forced break after it, two blocks that exactly fill page two): three blank pages before, and afterwards two pages with one square on the first and two on the second, which is what the file specifies. The `HtmlContainer.PageSize` passthrough is the other half — `HtmlContainerInt` has held the page and the surface apart all along and only the accessor outside the assembly was missing. **Deliberately NOT listed in `scripts/apply-pending-wpt-patches.sh`**: nothing in the repository paginates yet, so applying it to the WPT run would exercise nothing. It is here because the fix is correct and was expensive to find, not because anything is waiting on it — the paged renderer that would consume it is not landed, since enabling it still costs more tests than it wins (409 print reftests, 252 → 171: content on pages 2+ is frequently wrong, and bounding the paint to page one had been masking that). Verified with `git am --keep-cr` against the pinned pointer (`beab9f8`) — `HtmlContainerInt.cs` is pure CRLF and stays so (1592 CRLF, 0 bare LF after the apply); `HtmlContainer.cs` is pure LF. |
| `0136-css-accept-legacy-webkit-box-display` | `Broiler.CSS` | **`display: -webkit-box` and `display: -webkit-inline-box` were dropped as invalid values, so nothing downstream could tell a legacy WebKit flexible box from a plain block.** Eight lines: the two keywords join the closed set `IsAcceptableDeclarationValue` checks `display` against. **Why it matters is not the display itself** — a legacy box is approximated by `block`/`inline-block` (vertical) or `flex`/`inline-flex` (horizontal) in `CssUtils.NormalizeDisplayValue`, and for the vertical case, which is the common one, that is the same box the dropped declaration left behind anyway. It matters because **being a legacy box is the opt-in for `-webkit-line-clamp`**, and being one is *disqualifying* for the unprefixed `line-clamp` (a legacy box is a flex container, so `max-lines` does not apply to it). WPT `css-overflow/line-clamp` spends most of its `webkit-line-clamp-*` tests on one half or the other — `webkit-line-clamp-001` sets `-webkit-line-clamp` on a plain block and expects **no** clamping; `line-clamp-029` sets `line-clamp: 4` on a horizontal legacy box and expects all five of its lines — and neither is decidable while the keyword is dropped. **The main repository does not depend on this patch**, which is deliberate: `CssBox.IsLegacyWebkitBox` probes `CssDeclarationValidator` once for whether the linked engine keeps the keyword, and falls back to `-webkit-box-orient` (a property of the legacy box and of nothing else) while it does not. The fallback covers the `-webkit-line-clamp` opt-in, which needs a vertical orient anyway; what it cannot cover is the horizontal case, where there is nothing left to read. **Measured on the WPT reftest suite, `css/css-overflow/line-clamp` (263 tests), against the same tree with and without the patch: 174 → 175.** Three tests move to passing (`line-clamp-029`, `webkit-line-clamp-038`, `webkit-line-clamp-043` — all cases of a legacy box that must *not* clamp) and two move the other way (`webkit-line-clamp-002`, `-026`), both horizontal legacy boxes that now route to Broiler's flex path and hit its shrink-to-fit approximation of a flex container rather than rendering block-like. That trade is why the patch is worth applying and why the main repository does not wait on it. **Listed in `scripts/apply-pending-wpt-patches.sh`**: it decides whether a `display` declaration survives the cascade, so a mistake is visible only as pixels. Verified with `git am --keep-cr` against the pinned pointer (`56eea09`) — `CssStyleEngine.Values.cs` is pure CRLF and stays so (2290 CRLF, 0 bare LF after the apply). |
| `0135-css-media-queries-range-and-custom-media` | `Broiler.CSS` | **Two Media Queries gaps that made a query which should match not match — so the rules inside an `@media` block never cascaded and the page rendered as if they did not exist.** **Range syntax (Media Queries 4 §2.4):** `(width > 0px)` did not parse at all. `EvaluateMediaCondition` split a condition on `:` and nothing else, so the whole string read as one feature name, failed the `<ident>` test and evaluated as `<general-enclosed>` — false, and false under `not` too. All three forms are handled now — `<name> <op> <value>`, `<value> <op> <name>` (the same assertion with the comparison flipped) and the two-sided `<value> <lt> <name> <lt> <value>` — over **every** feature the evaluator already knew, not just `width`, because the feature dispatch was factored out into `EvaluateMediaFeature` and both the legacy and the range spelling now call it. **The strict comparisons are why `MediaFeatureRange` grows** rather than reusing `Min`/`Max`: `>` is not `min-`, and a viewport sitting exactly on the bound is the case that tells them apart — `(width >= 800px)` and `(width > 800px)` must disagree at 800. Range syntax has *no* prefixed spelling, so `min-width` appearing where a feature name goes is rejected rather than silently becoming the feature it prefixes. A comparison that is present but malformed (three operators, mismatched directions in the two-sided form, a non-ident in the name position) is `not all` and **stays false under `not`** — the one-line trap this patch was written around, since a parser that reports "not a range" for a malformed range hands the string back to the legacy path, which would then read `width>0px` as a feature name. **@custom-media (Media Queries 5 §3):** `@custom-media --foo (width > 0px);` already parsed into a `CssAtRule` that nothing ever read. Definitions are collected from every registered sheet into a `CustomMediaRegistry` keyed to `_sheetGeneration`, so it is rebuilt exactly when the rule index is and can never be stale against the sheets it came from; `(--foo)` resolves against it. **Collection is deliberately separate from evaluation** because a custom media query is document-global: a definition *below* the `@media` rule that references it still applies to it, so resolving in place while walking the rules would be order-dependent and wrong. `true`/`false` are honoured; an undefined name is *unknown* rather than false, so `not (--undefined)` does not match either (the `<general-enclosed>` rule); and a definition that reaches itself, directly or through another name, is a cycle — invalid, and guarded, because without the guard it recurses until the stack runs out. **Exit gate: 39 new `CssStyleEngineTests` cases** — both operand orders, both two-sided directions, non-length features (`aspect-ratio`, `resolution`), each malformed shape asserted *both* bare and under `not`, composition with `and`/`not`; and for `@custom-media`: resolution, definition-after-use, `true`/`false`, undefined, direct and mutual cycles. `Broiler.CSS.Dom.Tests` **426 passed** with the 2 pre-existing `CssDomArchitectureTests` failures (verified failing on a clean tree at the pinned pointer); `Broiler.CSS.Tests` 341/341. **WPT reftests:** `css/mediaqueries/at-custom-media-basic` passes, and a **458-test subset** over `css/mediaqueries`, `css/css-nesting`, `css/css-values` and `css/css-conditional` moves **301 → 302 with no test changing the other way**. **Listed in `scripts/apply-pending-wpt-patches.sh`**: this decides whether an `@media` block's rules cascade at all, so a mistake is a whole stylesheet applying or not applying to a page — the unit tests pin the grammar, only the pixel suite can say the rules reached the render. Verified with `git am --keep-cr` against the pinned pointer (`c3d5a73`): `CssStyleEngine.Values.cs` is pure CRLF and stays so (2283 CRLF, 0 bare LF after the apply), the other two files pure LF. |
| `0134-html-pixel-diff-no-png-roundtrip` | `Broiler.HTML` | **`PixelDiffRunner.Compare` stops PNG round-tripping both bitmaps before it reads a pixel.** The WPT runner's second-largest phase, **16–21% of a run** at 327–501 ms per comparison of two 1024x768 bitmaps ([`render-fixed-cost.md`](../tests/render-stages/results/render-fixed-cost.md)). **The obvious culprit was not the culprit:** the per-pixel `GetPixel`/`SetPixel` loop reads far worse and is real, but it was **~22 ms of 284** — the other **92% was `NormalizeForComparison`**, running `BBitmap.Decode(source.Encode(Png, 100))` on *both* inputs, a full PNG compress and decompress of 3 MB each, per comparison. **The round trip was an identity, and that was measured rather than argued**: a synthetic opaque image, one with graded alpha, one fully transparent with non-zero RGB (the two cases where a codec may premultiply or collapse the colour type), and 25 real WPT reference PNGs off disk all round-tripped byte-identical. It could not have been otherwise — `Encode` serialises the same `_pixels` array `GetPixel` reads, so a lossless round trip has nothing to normalise between two `BBitmap`s, and the method's own `catch (InvalidOperationException) => source.Copy()` fallback already treated a plain copy as acceptable. **Three changes:** the inputs are compared directly; the loop reads the backing spans (`BBitmap.PixelBytes`, assembly-internal, the same store `GetPixel` reads) instead of a call and a `BColor` 1.57 M times; and the diff bitmap is built in a **second pass only when the comparison failed**, instead of being allocated and written for every pixel of every comparison and discarded on the match path — a 3 MB image and 786 432 `SetPixel` calls nothing looked at, on the ~62% of tests that pass. **Measured: match path 284.61 → 4.50 ms (62×), mismatch 285.23 → 7.92 ms (36×); `css/css-backgrounds` reftests at 4 workers 368.5 → 297.3 s (1.24×)** — less than the 186 CPU-s removed, because four workers divide it. **Identical results, both ways:** WPT reftest classification 444/266/1 before and after with the *failing-test sets identical name for name*, and `Broiler.Wpt.Tests` 748 passed / 57 failed both ways with failure sets identical name for name (those 57 pre-existing on this host). **One scare, recorded because it was a false alarm worth remembering:** the first post-fix run differed by three `background-size` tests; restoring normalization while keeping the loop rewrite reproduced the same three, and running the **pristine** tree with the same invocation produced exactly the fix's 266-failure set — so they are a pre-existing nondeterministic fringe, and the runs they had been compared against were taken back-to-back inside one script invocation. **Not listed in `scripts/apply-pending-wpt-patches.sh`**: it changes no pixel and no classification, and it would only make CI's own numbers faster, which is not what that script is for. Published: [`pixel-compare.md`](../tests/render-stages/results/pixel-compare.md). Verified with `git am --keep-cr` against the pinned pointer (`54fc320`); both files are pure LF and stay so. |
| `0133-html-layout-pass-counter` | `Broiler.HTML` | **`HtmlContainerInt.PerformLayout` counts the full-tree layout passes it performs.** Multithreading roadmap Phase 4, the measurement its step 1 needs — **4 lines**, because the type it calls (`Broiler.Layout.Diagnostics.LayoutPassCounter`) is in the main repository, so `tests/render-stages` compiles and runs whether or not this is applied (the pass column just reads zero, and the run says so). **Why a counter and not a timer.** The `Broiler.Layout` roadmap's step 1 — "stop laying out the whole tree twice" — is stated as the highest-value sequential item in Phase 4, "worth more than steps 3 and 4 combined", and names the site exactly: `PerformLayout` runs `Root.PerformLayout` a second time whenever `MaxSize.Width <= 0.1`, so the first pass can discover the shrink-to-fit width the second lays out against. That reading of the source is correct; what nothing had established is *which hosts reach the branch*. A stage time cannot answer it — 50 ms of layout is one pass of 50 or two of 25 — and inferring the count from `MaxSize` in the harness would only restate the harness's own assumption about `MaxSize`. So the increment is at the site, immediately before each `Root.PerformLayout`, plus one on entry to `PerformLayout` itself so that a *caller* that lays out three times (as `HtmlRendererUtils.MeasureHtmlByRestrictions` does) is distinguishable from an engine that does. **What it measured** (`--layout-passes`, published as [`tests/render-stages/results/layout-passes.md`](../tests/render-stages/results/layout-passes.md)): every fixed-viewport row is **one call, one pass** — the branch is unreachable from the WPT runner, the CLI capture, the browser and every benchmark in this repository, all of which set a viewport width. It fires for the auto-size embedding path, where it is **two passes from one call** and costs **0.80–1.35×** the single-pass layout, not 2× — the first of the two runs at width 99999 where the line breaker barely runs, so the pass step 1 would cache away is the cheaper of the two. **Off by default in production**: `Record` reads one static `bool` and returns; no allocation, nothing to synchronise. The counters are `[ThreadStatic]`, so a render reads its own. **Exit gate:** 4 `LayoutPassCounterTests` cases, including the thread-affinity one that fails silently if it is ever got wrong — and the harness itself asserts the count is identical across every iteration of a shape, so a count that varied with carried-over state would abort the run rather than be medianed away. **Deliberately NOT listed in `scripts/apply-pending-wpt-patches.sh`**: it is measurement-only and cannot move a pixel, so applying it there would exercise nothing. Verified with `git am --keep-cr` against the pinned pointer (`54fc320`) — the file is pure CRLF and stays so. |
| `0132-html-cascade-invalidation-set` | `Broiler.HTML` | **`HtmlContainerInt` hands the render-tree ledger the stylesheets it just cascaded, so a connected attribute write nothing can see is answered without a rebuild.** Multithreading roadmap item #14, second half — 12 lines, because the type it calls (`Broiler.Layout.Engine.CascadeInvalidationSet`) is in the main repository. **What it completes:** `patches/0131` (upstream) taught the container to ask *whether the mutated node hangs off the document*, which elides every offscreen edit and nothing connected. The row that left on the table was measured: one `data-*` write on a connected element that no corpus selector can reach still cost **997.8 ms** on the rule-heavy page, for a whole-document re-cascade producing the page that was already on screen. **What the set is:** every class, id and attribute name any selector mentions *anywhere* in its text, plus every attribute any declaration reads through `attr()`. The scan is flat and that is what makes it sound under combinators — `.a .b` is affected by a class write on an ancestor, `[hidden] + p` by one on a sibling, `:has([open])` by one on a descendant — so a name that appears nowhere cannot change any match, whatever the combinators, and no combinator is modelled. Anything the scanner does not model (an escaped identifier, `[svg\|href]`) makes the whole set conservative and every attribute affecting, which is the behaviour that was there before it. **The cascade's answer is not sufficient on its own**, which is the half the item's wording does not contain: box construction reads `src`, `href`, `colspan` and the presentational attributes without going through a selector at all, so the elision needs *both* halves to say no and the box half is an **allow-list** (`class`, `data-*`, `aria-*`, `role`) rather than a deny-list. Two carve-outs inside it came from reading the engine: `data-broiler-*` is the script bridge's own channel into layout (scroll offsets, top-layer order, backdrops, frame documents) and is excluded; and nothing inside an `<svg>` subtree is inert, because `FragmentTreeBuilder` serializes it back to markup attribute by attribute for a renderer with its own idea of what a `class` means. `id` is deliberately absent — the box tree carries it for anchor resolution and the PDF link map. **Measured, paired against the pinned pointer on one host** (`--relayout-profile`, 4 cores, medians of 5): the `unstyled attribute` row goes **1 476.7 → 43.0 ms (34.4×)** on `rules`, 245.9 → 17.3 (14.2×) on `paint`, 179.5 → 13.0 (13.8×) on `mixed`, 275.9 → 29.1 (9.5×) on `boxes`, 179.6 → 85.9 (2.1×) on `text`; a class toggle whose token the sheet does not name is **36.0×** on `rules`, 12.2× on `paint`, 9.4× on `mixed`. Rows that still rebuild span 0.79–1.21, this host's run-to-run spread. **The two control rows are the evidence that it is the sheets doing the work**: `styled attribute` writes `data-k` and still rebuilds on `rules` — the only corpus page whose sheet mentions it — and `styled class` still rebuilds on `rules` and `boxes`, the pages whose sheets name the token being written. Same mutation, opposite decision, decided by the document. **Exit gate:** `--relayout-parity`, which renders every page after every mutation with the elision on and off and compares the images byte for byte (45 pairs, 22 elided, all identical), and which fails a run in which nothing was elided; plus 31 `RenderTreeInvalidationTests` and 26 `CascadeInvalidationSetTests` cases, weighted towards the "must rebuild" side because a false "no rebuild" is a stale render and a false "rebuild" is only slow. **That gate failed on first run, and what it found was not this patch:** three `boxes` rows differed, one of them a row `0131` already elided. Laying the same box tree out twice was never idempotent — `CollapsedMarginTop` carried a previous pass's decision, so a first child's top margin stopped propagating and the page laid out two pixels short. Fixed in the main repository (`CssBox.ResetCollapsedMarginState`, `LayoutIdempotenceTests`); the drift had been live in `0131` since it shipped. `Broiler.Layout.Tests` 353/353, and the render-bearing half of `Broiler.Cli.Tests` — Acid, WPT, `GoogleSearchPolyfill`, form-control and CSSOM, 928 tests — **run in full both ways on one host with failure sets identical name for name** (41 failures each way, 40 unique names, none added and none fixed; all pre-existing on the pinned pointer and environmental in character). The full assembly is not the comparison here because it aborts partway on this host for memory reasons, in both directions. **Listed in `scripts/apply-pending-wpt-patches.sh`** in `0131`'s place — a wrong classification here is a stale page, which is precisely what a pixel-comparing suite exists to catch. **Applies on top of `0131`**, which is upstream in the pinned pointer; verified with `git am --keep-cr` against that pointer. |

`0128` (`Broiler.CSS`, cache sharding), `0129` (`Broiler.HTML`, cascade sub-stage
trace and warm pass), `0130` (`Broiler.JS`, test parallelization) and `0131`
(`Broiler.HTML`, the invalidation ledger this patch's other half calls) were the
previous entries. All four are upstream and their submodule pointers are bumped, so
their fixes reach CI through the pointer rather than through
`scripts/apply-pending-wpt-patches.sh`; their entries in that script are removed
with them, because an entry whose idempotence guard can only ever skip is noise.
Their patch files are deleted for the reason stated at the top of this file: the
directory is a backlog, not an archive.

`0131` is the one to check first if `0132` ever fails to apply — they touch the same
three lines of `HtmlContainerInt`, and `0132` is written against a tree that already
has `0131` in it.

**How to tell, rather than assume.** A patch is upstream when it *reverse-applies*
to the pinned pointer's tree — `cd <Submodule> && git apply --reverse --check
../patches/NNNN-<slug>.patch` succeeding means the fix is already there. That is
the same test `apply-pending-wpt-patches.sh` uses for its idempotence guard, and
it is worth running over the whole directory at the start of a task: a patch that
has landed since the file was last edited is otherwise indistinguishable from one
that has not, and re-applying it fails confusingly rather than harmlessly.

**One way that check lies, and it lied here.** A reverse-check also fails when the
paths in the patch do not resolve, which looks exactly like "not upstream" and is
not the same claim. `0130` was generated from a tree whose paths carried an extra
leading directory, so the check needs `-p2`; at the default strip level it reports
*"No such file or directory"* and the fix was upstream all along. Read the failure
before believing it, and settle it by reading the target file — which is what
established all three of the retirements above.

<!-- Retired index rows, kept for the record of what 0128, 0129, 0130 and 0131
claimed and measured:

| `0131-html-relayout-invalidation-ledger` | `Broiler.HTML` | **`HtmlContainerInt` asks what changed before it rebuilds the render tree, instead of only whether something did.** Multithreading roadmap item #14, first slice — 30 lines, because the type it calls (`Broiler.Layout.Engine.RenderTreeInvalidation`) is in the main repository. **What it replaces:** `EnsureBoundDocumentCurrent` compared a bound `DomDocument`'s `Version` against a copy and, on any difference, called `BuildBoundDocument` — which disposes the render tree and regenerates box tree and full cascade before the layout pass runs. The relayout profile measured that rebuild at **60–97% of a relayout** (97.1% on the rule-heavy corpus page), so the version counter was the most expensive boolean in the engine: it says "something changed" and nothing else. **The blocker the roadmap named did not exist.** Item #14 was recorded as starting in `Broiler.DOM`, "giving mutations a shape a consumer can act on". `DomDocument.Mutated` has published a typed `DomMutationRecord` — type, target, added and removed nodes, attribute name, old and new value — for as long as `MutationObserver` has worked. What was missing was a consumer, and it is on this side. **What it elides, and why that is provable rather than heuristic:** one rule — a record whose target does not hang off the bound document cannot have changed anything the render tree shows, because the box tree is built by walking that document and nothing else. `ChildList` records name the *parent*, which is what makes the test sound in both directions: nodes added to a detached parent are themselves detached, and a node moved *out* of the page is reported against the still-connected parent it left. **What it deliberately does not elide:** anything connected, including a `data-*` write no selector can reach — that needs the cascade to answer whether a rule's subject could match differently, which is the rest of the item. **The backstop is what makes it safe against the case it cannot see:** the ledger records the version each record arrived at, and answers "rebuild" unconditionally if the counter has moved further than the records account for (an unobserved publish path, an earlier subscriber that threw, a document mutated before it was bound). An elision is only possible when every bump since the last build is accounted for. **Measured, paired against the pinned pointer on one host** (`--relayout-profile`, 4 cores, medians of 5): the offscreen-build row goes from a full rebuild to none on all five corpus pages — `rules` **1 032.7 → 11.5 ms (89.8×)**, `boxes` 264.2 → 10.3 (25.8×), `paint` 224.8 → 9.9 (22.6×), `mixed` 116.9 → 7.2 (16.3×), `text` 117.2 → 11.7 (10.0×). The other thirty rows span 0.82–1.27 with no page and no mutation systematically on one side, which is that host's run-to-run spread: the ledger changes what is skipped, not what is done. **Exit gate:** 16 `RenderTreeInvalidationTests` cases, weighted towards the connected side because a false "no rebuild" is a stale render and a false "rebuild" is only slow; plus the harness's own `rebuilt?` column, which reports the decision rather than leaving it to be inferred from a time. **`Broiler.Cli.Tests` run in full both ways on one host — 2 931 tests, 82 failures, and the two failure sets are identical name for name** (77 unique names; nothing new, nothing fixed). Those 82 are pre-existing on the pinned pointer and environmental in character — the PDF-converter tests `CLAUDE.md` warns about, the Skia/WebClient architecture guards, the Acid image comparisons. `Broiler.Layout.Tests` 308/308. **Listed in `scripts/apply-pending-wpt-patches.sh`** — a wrong classification here is a stale page, which is precisely what a pixel-comparing suite exists to catch and what no unit test over the classifier can reach. |

| `0128-css-style-engine-cache-sharding` | `Broiler.CSS` | **The style engine's three memo caches came off the single `_sync` lock, and a fourth was added so a warm pass has somewhere to publish.** Multithreading roadmap item #12, step 2. `_cache`, `_sparseCache` and `_declaredCascadeCache` were plain dictionaries behind one `Monitor`, which made every cache probe on the cascade's hot path a lock acquire — the shortest possible critical section and therefore the one with the worst work-to-synchronisation ratio once more than one thread cascades. They are `ConcurrentDictionary` now. **What deliberately did not change is the generation-guard protocol**: a cascade still captures `_cacheGeneration` up front and still publishes under `_sync`, so "is this result still current" and "publish it" cannot be split by a concurrent `InvalidateAll`, and `InvalidateAll` still clears inside the lock even though the caches no longer need it for their own sake. That guard is what makes the lock-free compute window correct, so it survives by intent rather than by omission. **The fourth cache is the point of the patch.** `GetCascadedStyle` memoized nothing at its own level — only its inner declared cascade did — so custom-property and `var()` resolution, CSS-wide keywords, shorthand expansion, `attr()` substitution and relative font weights were recomputed on every call. Within one render that saves nothing, because a render asks for each element once; it exists as the store `0129`'s warm pass writes into. **Exit gate:** `CssStyleEngineConcurrencyTests` grows a second case, and it tests a different thing from the first — the existing one says the caches survive concurrent access and never compares a value, so it cannot say the *answers* do. The new one takes a sequential cascade over one document and an eight-thread cascade over a second, structurally identical one, and compares property by property. `Broiler.CSS.Dom.Tests` 384 passed with the two `CssDomArchitectureTests` failures that are pre-existing (verified on a clean tree), `Broiler.CSS.Tests` 341/341. **Must be applied with `0129`** — that patch's warm pass is what makes this one do anything. |

| `0129-html-cascade-substage-trace-and-warm-pass` | `Broiler.HTML` | **`parse+cascade` is timed sub-stage by sub-stage, and every element's cascade is resolved on a thread budget before the box walk consumes it.** Multithreading roadmap items P0-a (the split Phase 2 §9 said the document owed) and #12. Small — one file, two call sites — because both types it calls live in the main repository (`Broiler.Layout`), the way `TileParallelReplay` does for item #5, so the main repo builds and its tests compile whether or not this patch is applied. **The measurement half:** the four sub-stages are wrapped in `RenderStageTrace` scopes, which are off by default and cost a static `bool` read and a null-check `Dispose` when they are. It had to be instrumentation rather than P0-a's usual out-of-band subtraction, because none of the four is a pure function of the source — each consumes the tree and style set the previous one produced. The answer: **the cascade is 81.3–98.2% of the stage on every corpus page and both parse halves together are 0.5–4.8%**, so the stage's name overstates the parse on every page including the one with 211 592 characters of source. **The threading half:** `CssStyleRecalc.Warm` runs before `CascadeApplyStyles`. The obvious reading of item #12 is to thread that walk over sibling subtrees, and the walk cannot be split without changing what it produces — it rewrites `display` when `float` is set so children observe the corrected value, pushes `text-decoration` down onto children, hides a closed `<details>`'s subtree *after* cascading it, and inserts generated `::before`/`::after` boxes into child lists on the way back up, the last two writing to nodes the walk has already left. The expensive part is not in the walk anyway: per box it is one `GetCascadedStyle` call, which reads the canonical DOM and the registered stylesheets and mutates neither, so it is a pure function of state that is already final. The warm pass resolves all of it on `BROILER_STYLE_THREADS` and the walk below is byte-for-byte the walk that was there, reading cache hits. **Measured: cascade stage 1.16–2.16× at four threads, 1.06–2.10× end to end**, with the harness now publishing the serial residue per page (16–55%) so what is left is stated rather than guessed. **Exit gate:** pixel-identical at 1/2/4 across the corpus (`--style-scaling`, which fails the run on a single differing byte) and at 1/2/3/4/8 over 22 `ParallelStyleRecalcTests` cases across five documents chosen for what makes an element's cascade depend on something other than itself, with a guard test asserting each document clears the warm pass's element threshold so no equality assertion can pass vacuously. **Must be applied with `0128`**, which supplies the memo the warm pass writes into; without it the pass computes correct results and discards them, which is correct and pure overhead. |

| `0130-js-tests-reenable-parallelization` | `Broiler.JS` | **`Broiler.JavaScript.BuiltIns.Tests` runs its cases in parallel again.** Multithreading roadmap item #21. The assembly set `[assembly: CollectionBehavior(DisableTestParallelization = true)]`, and the roadmap's guess about why was right: the engine used to dispatch promise continuations, async-function resumptions and generator steps onto `ThreadPool` threads whenever no synchronization context was installed — which is exactly the situation in a unit test — so two tests at once could have their continuations interleaved onto each other's ambient context. Item #15 removed that dispatch. What makes it safe rather than merely no-longer-obviously-unsafe is that a context is reachable from exactly one thread: `JSEngine.Current` is `[ThreadStatic]` and the async-local mirror that restores it across await points is `AsyncLocal`, so two xUnit threads each holding their own `JSContext` cannot see each other's at all. What they do share is process-wide and already concurrent — interned key strings, the built-in registry's static constructors (serialised by the CLR), and `DictionaryCodeCache.Current`, whose per-key compilation is serialised by `Lazy<T>`. **Measured: 2 118 tests, 0 failures, 57–59 s serial against 31–37 s parallel on four cores, three runs each (~1.75×).** The file keeps a note on what to look for if it ever has to go back: a new piece of process-wide mutable state, not a new test. **Deliberately NOT listed in `scripts/apply-pending-wpt-patches.sh`** — the WPT run never builds this assembly, so applying it there would exercise nothing. |

-->

<!-- Retired index note, kept for the record:

**Empty — nothing was pending.** `0123-css-cascade-rule-index` was the last entry;
it is upstream as `Broiler.CSS` `377c6dd` on `claude/css-cascade-rule-index` and
the submodule pointer is bumped, so the fix now reaches CI through the pointer
rather than through `apply-pending-wpt-patches.sh`. Its `PENDING_PATCHES` entry is
removed for the same reason: the script's idempotence guard would have skipped it
from here on, and an entry that can only ever skip is noise.

-->

<!-- Retired index row, kept for the record of what 0123 claimed and measured:

| `0123-css-cascade-rule-index` | `Broiler.CSS` | **The cascade tested every rule of every sheet against every element; it now tests only the rules an element's own id, classes and tag can reach.** Multithreading roadmap item #11 — the item that document's Phase 0 measurements identify as its highest-value one, ahead of every parallel item including the rasterizer, and single-threaded work. The evidence it was written against: on the roadmap corpus's rule-heavy page (700 `<div>`s, two classes each, against a 900-rule sheet) `parse+cascade` was **96.5% of a 5.2 s render**, and `RuleScalingBenchmarks` — which holds the *matched* rule count fixed at four while growing the sheet — measured cost and allocation both linear in **total** rules: 32× the rules gave 30.8× the time and 32.0× the bytes. `CssCascadeRuleIndex` flattens the sheet set into document order once and files each rule under the simple selector its subject must carry (id / class / type), with everything else in a universal bucket; an element merges the buckets its own keys reach and tests those. **The correctness argument is one-sided by construction and that is the whole point**: a key is a *necessary* condition for a match, never a sufficient one, candidates still go through the unchanged selector matcher, so the index can only be wrong by *omitting* a rule — and every case that is not certainly narrower resolves to the universal bucket (a bare `:is()`/`:not()`, an attribute-only compound, a namespaced type, an escaped identifier, anything the key scanner does not model). Candidates come back in **document order**, via a k-way merge of the buckets rather than a sort, so the cascade's source-order tie-break is untouched — a rule that never matched never advanced the tie-break counter, so visiting only candidates yields the same winners. `@media`/`@supports` are resolved once at index-build time (they depend on the environment and the feature oracle, both fixed for an index's lifetime) so a non-applying group costs nothing per element; `@container` depends on the element, so those conditions travel with the entry and are evaluated per candidate. The index is memoized against a **sheet** generation bumped only by sheet/environment changes — deliberately not by DOM mutation, which bumps the computed-style caches constantly and would otherwise rebuild the index on every tree edit. **The linear scan is kept and kept reachable** behind `CssStyleEngine.UseRuleIndex`, as the index's oracle: `CssCascadeRuleIndexTests` (22 cases) asserts the two cascades agree property by property — id/class/type keying, source-order tie-breaks, a rule reachable through two keys, combinators, pseudo-elements, the six unkeyable selector shapes, matching and non-matching `@media`, `@supports`, nested groups, late-added sheets, `ClearStyleSheets`, a viewport change — and over a generated 1200-rule sheet against a 120-element document. **Measured, and the exit gate is the scaling shape rather than a single number**: with `RuleScalingBenchmarks` holding the matched set at four and growing only the rules that cannot match, 32× the rules used to cost 30.8× the time and 32.0× the bytes and now costs **1.64× the time and 1.13× the bytes** — 114.9 ms → 15.74 ms at 100 rules, 3 543.6 ms → **25.89 ms** at 3 200 (136.9×), with allocation 5 817.9 MB → **9.69 MB** (600×). On a whole render the corpus `rules` page goes **5 218.96 ms → 1 841.71 ms** end to end and its `parse+cascade` stage **5 035.35 ms → 1 698.11 ms** (2.8× and 3.0×) — the smaller figure is Amdahl, since that stage also parses 110 296 characters and then cascades the rules that do match. **No rendering change**: `css/css-backgrounds` + `css/CSS2/linebox` (62 tests) is identical before and after — same pass/fail/skip on every test and the same 98.62245927777205% average pixel match to fourteen digits. Component suites: `Broiler.CSS.Tests` 341/341, `Broiler.CSS.Dom.Tests` 383 passed with the two `CssDomArchitectureTests` failures that are **pre-existing** (verified on a clean tree: 361 passed, the same 2 failed). Applies cleanly to the pinned `Broiler.CSS` pointer (`edc9fa9`) and survives `git am --keep-cr` — verified by applying it to a fresh clone of the pin and diffing the result against the branch it was generated from (identical tree). **Listed in `scripts/apply-pending-wpt-patches.sh`**: unlike a thread-safety or scheduling patch, this one *could* move rendering if the key extraction were ever wrong, so having the WPT run exercise it against the pinned pointer is the check worth having — and its claim is precisely that the numbers do not move. |

-->
