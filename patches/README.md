# Pending submodule patches

Fixes that belong in a submodule (`Broiler.HTML`, `Broiler.CSS`, `Broiler.DOM`,
`Broiler.JS`, `Broiler.Graphics`) but could not be pushed to their remote from
the session that wrote them: the git proxy only authorises repos in the session's
GitHub scope, so a push to a submodule remote outside it returns **403**. Rather
than bump a submodule pointer at a commit CI cannot clone, the change is captured
here as a `git format-patch` file for a maintainer to apply.

The directory is a backlog, not an archive: it holds only what is *currently*
pending. A patch is deleted from here, along with its row below, once its fix is
upstream and the submodule pointer is bumped.

## Applying

```sh
cd <Submodule>
git am --keep-cr ../patches/NNNN-<slug>.patch
git push origin HEAD
cd ..
git add <Submodule>        # bump the pointer only after the push succeeds
```

**`--keep-cr` is not optional for a patch that touches a file with CRLF line endings**, which
several `Broiler.JS` sources are (mixed CRLF and LF, within one file). `git am` runs the patch
through `mailinfo`, which normalizes the line endings of the diff body unless told not to — so the
context lines stop matching the file and the apply fails with *"patch does not apply"* on a patch
that is perfectly good. `git apply` does not have the problem, which is exactly why it is not the
check: these instructions use `am`, so `am` is what a patch has to survive. Verified per patch by
applying it to a clean checkout of the pinned pointer and diffing the result against the branch it
was generated from.

## Exercised on CI before they land

`scripts/apply-pending-wpt-patches.sh` applies the patches listed in its
`PENDING_PATCHES` array to the checked-out submodule trees before the WPT run, so
a pending fix is reflected in CI's numbers rather than waiting on the pointer. It
is idempotent — a patch already contained in the pinned pointer reverse-applies
and is skipped — so an entry stops applying by itself once a maintainer lands it.

## Index

| Patch | Submodule | Note |
| --- | --- | --- |
| `0102-css-dir-pseudo-class` | `Broiler.CSS` | **`:dir()` matched every element, in both directions at once.** It was listed in `CssSelectorMatcher`'s `RecognizedPseudoClasses` but had no arm in the pseudo-class switch, so it fell through to the deliberately-lenient default for recognised-but-unmodelled names — which means `:dir(ltr)` *and* `:dir(rtl)` applied to the whole document. That is how a single shadow-tree rule painted an entire canvas in issue #1538 problems 16 and 20 (`css/css-shadow/shadow-directionality-001` and `-002`, both at ~1% match). It now resolves HTML's directionality concept: the nearest ancestor-or-self with a valid `dir` attribute, `ltr` at the root, with `dir=auto` — and `<bdi>`, whose default it is — resolved from the first strong directional character, skipping `<script>`/`<style>` and any descendant that declares its own direction. An argument other than `ltr`/`rtl` matches nothing (Selectors 4 §11.2). **Strictly a narrowing**, so it can only remove matches the lenient default invented. **The two tests it is named for do not depend on it**: they are carried over the threshold by the main-repo shadow-tree scoping that shipped with it (1.1–1.3% → 99.55%, measured), and this makes them pass for the right reason rather than because every `:dir()` matched. 7 tests in `CssSelectorMatcherDirTests` cover both directions, inheritance, an invalid attribute value, `auto` in three shapes, the `<bdi>` default and the non-`ltr`/`rtl` argument. Applies cleanly to the pinned `Broiler.CSS` pointer (`076ed5d`) and survives `git am --keep-cr`. Listed in `scripts/apply-pending-wpt-patches.sh`, so the WPT run exercises it on CI ahead of a maintainer landing it. |
| `0103-js-boxing-conversion-site-census` | `Broiler.JS` | **Where the corpus's boxing conversions are minted, attributed to the compiler site rather than totalled** (`docs/performance-roadmap.md` item 3-1). §4.2a re-opened item 3-1's typed backing store on a conversion count that had tripled — 24.6 M → 69.3 M once the census stopped silently running 7 of 15 suites — and whose dominant producer had changed identity to Gameboy. `0113` then settled the storage question from the other side by counting the read/write ratio, but nothing had ever said *where* the conversions come from, because the counter sits in the boxing factory and sees only that a raw `double` crossed into a `JSValue`. Each of the compiler's 21 boxing sites now names itself and the census reports the split; the site travels as a constant argument on the one code path the engine ships, rather than as a factory entry per site or an argument gated behind the counter flag, which would leave the measured arm and the shipping arm running different code. **Counted over all 15 suites: 61.8% of 69.3 M conversions are the guarded tree's ROOT box, and the generic fallback arm it can leak to is 226 of 69.3 M** — the tree is not leaking, and what is left is the one box per evaluation the design keeps. Two defects found on the way: `NumberBoxingDiagnostics.Reset` never cleared item 3-8a's speculative-read counter, so a host resetting between suites carried it forward and every suite but the first read high; and the census ran its suites in one process in a fixed order, so Mandreel taking the process down cost every suite after it — the suites are now selectable, and Gameboy, Typescript, Box2D, zlib and CodeLoad have rows for the first time. 7 cases in `NumberBoxingConversionSiteTests` prove the instrument discriminates on constructed shapes before any corpus number is read from it, the load-bearing one being that the eight sites partition the factory's own independent conversion total. **Not listed in `apply-pending-wpt-patches.sh`**: it is an engine instrument and changes nothing WPT renders. Applies cleanly to the pinned `Broiler.JS` pointer (`8308df51`) and survives `git am --keep-cr`. |
| `0104-js-legacy-frame-out-parameter` | `Broiler.JS` | **The Annex B `caller`/`arguments` frame handed back through an `out` parameter instead of returned by value** (`docs/performance-roadmap.md` item 4-5). `0110` attributed 92% of a call's bookkeeping to this frame — 44 ns on 60.16% of the corpus's calls — and `0111` priced the item's named fix, relocating the frame to a thread-local stack, at 0.20% and refused it; its third arm is the reason, since one 56-byte `Arguments` copy alone is 8.19 ns, so *the cost is the copying and relocating where the copying lands does not remove it*. `PushLegacyFrame` returned a 72-byte `LegacyFrame` by value and the caller assigned it; an `out` parameter writes it straight into the caller's local, which is the same displaced frame with two fewer copies and no semantic change. **Measured in situ with `--call-entry-cost`, twelve interleaved ABBA pairs against a baseline binary built from the unmodified tree on the same machine: `InvokeFunction` 117.32 → 115.50 ns and the frame itself 44.05 → 42.02, ahead on 9 of 12 pairs — 1.83 ns on 59 372 476 invocations, 109 ms of a 108 879 ms driver, 0.100%.** 9 of 12 is not the separation §3.5 requires to claim a win, and the patch is kept because it is strictly less work for identical semantics rather than because 0.100% is established. **The gap between the two numbers is the finding worth keeping**: `0111` priced a struct copy at 8.19 ns and this removes two of them for 1.83, so most of the return-by-value traffic was already elided before it reached the machine. Annex B behaviour unchanged — `LegacyCallerArgumentsTests`, 14 cases, and 8 057 tests across six projects with no failures. **Not listed in `apply-pending-wpt-patches.sh`** for the same reason as `0103`. Applies cleanly to the pinned `Broiler.JS` pointer (`8308df51`) and survives `git am --keep-cr`. |
| `0105-js-root-box-consumer-split` | `Broiler.JS` | **What consumes the guarded tree's root box, and it is the numeric local rather than either storage item** (`docs/performance-roadmap.md` item 3-1). `0103` counted the root at 61.79% of the corpus's conversions and left one question: the root is boxed only because its consumer takes a `JSValue`, so the only thing that removes it is a consumer able to take the raw `double` the tree already holds — and that is knowable only to the compiler. The consumer travels with the node being visited, and **only to a node that *is* an `AstBinaryExpression`**, the one shape reaching the tree builder directly, so it can never survive into a nested visit; the builder clears it for its own construction because a leaf may contain a tree, and a compound assignment does not claim its right-hand side because there the tree is an operand rather than the value stored. **Of 42 849 742 root boxes over all fifteen suites: local 44.36%, element 17.91%, property 13.14%, argument 4.43%, return 0.40%, unattributed 19.77%.** A proven-numeric local already has a raw `double` home (item 3-3), so a root landing there is one the existing numeric tier failed to type — **the typed backing store's entire ceiling on root boxes is the element row, 11.07% of the corpus's conversions**, against `0113`'s finding that the store is an allocation wash at a 3.34 read/write ratio. Per suite: Crypto 81.8% local, Typescript 74.0% property, NavierStokes 59.2% element, Splay 98.5% argument. The 19.77% residual is the assignment forms this pass does not wire and is reported rather than folded away; **Gameboy carries 43% of its roots there**, so its row is the least trustworthy in the table. 7 further cases, the load-bearing one being that a consumer does not leak into a tree nested inside its own right-hand side; 8 162 tests across eight projects, no failures. **Applies on top of `0103` and `0104`, in that order** — not standalone. **Not listed in `apply-pending-wpt-patches.sh`**: engine instrumentation, nothing WPT renders. Survives `git am --keep-cr`. |
| `0106-js-numeric-local-refusal-weighting` | `Broiler.JS` | **Which numeric-tier refusal costs the boxes, counted by execution rather than by declaration** (`docs/performance-roadmap.md` item 3-1). `0105` put 44.36% of the corpus's guarded-tree root boxes into a local and left two explanations. The cheap one — a *seam*, where the tier had already accepted the destination and the box is minted by the tree and unboxed by the next instruction — **is measured and refuted at 36 boxes of 18.6 M**. So every one is a local the tier *refused*, and item 3-6 had already counted the refusals in the wrong currency: **per name**, which weights a name refused in initialization code the same as one refused inside a ten-million-iteration loop. The analysis now retains its per-name refusal and the boxing site names it. **Of 19 005 731 such boxes over all fifteen suites: `DroppedCandidate` 38.41%, `ElementRead` 36.35%, `Unknown` 12.78% (a gap in the instrument, reported as one, not a cause), `PropertyRead` 4.87%, `Parameter` 4.08%, `CallResult` 1.44%.** The largest row has **no independent cause to fix** — a cascade is refused only because another refused name reaches it, and NavierStokes at 96.8% cascade is the roadmap's own already-recorded `rowSize` finding arriving from a second direction. **The largest independent cause is `ElementRead`** (58.1% of Crypto's): item 3-1's guarded tree already proves `a[i]` numeric *at run time* and the local's analysis refuses it *statically*, so the box is the cost of the two mechanisms not sharing a conclusion. The obvious fix is item 3-8a, which was built complete and closed as a regression, so what this hands forward is the read/write ratio for this population — counted before anything is built, per §3.5. 4 further cases, the load-bearing one being that the refusals partition the boxes counted at the site; 8 166 tests across eight projects, no failures. **Applies on top of `0103`, `0104` and `0105`, in that order** — not standalone. **Not listed in `apply-pending-wpt-patches.sh`**: engine instrumentation, nothing WPT renders. Survives `git am --keep-cr`. |
