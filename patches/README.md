# Pending submodule patches

Fixes that belong in a submodule (`Broiler.HTML`, `Broiler.CSS`, `Broiler.DOM`,
`Broiler.JS`, `Broiler.Graphics`) but could not be pushed to their remote from
the session that wrote them: the git proxy only authorises repos in the session's
GitHub scope, so a push to a submodule remote outside it returns **403**. Rather
than bump a submodule pointer at a commit CI cannot clone, the change is captured
here as a `git format-patch` file for a maintainer to apply.

## Applying

```sh
cd <Submodule>
git am --keep-cr ../patches/NNNN-<slug>.patch
git push origin HEAD
cd ..
git add <Submodule>        # bump the pointer only after the push succeeds
```

Delete the patch file and its row below once the pointer is bumped.

**`--keep-cr` is not optional for a patch that touches a file with CRLF line endings**, which
several `Broiler.JS` sources are (mixed CRLF and LF, within one file). `git am` runs the patch
through `mailinfo`, which normalizes the line endings of the diff body unless told not to — so the
context lines stop matching the file and the apply fails with *"patch does not apply"* on a patch
that is perfectly good. `git apply` does not have the problem, which is exactly why it is not the
check: these instructions use `am`, so `am` is what a patch has to survive. Verified per patch by
applying it to a clean checkout of the pinned pointer and diffing the result against the branch it
was generated from.

## Index

| Patch | Submodule | Note |
| --- | --- | --- |
| `0111-js-legacy-frame-shape` | `Broiler.JS` | **Item 4-5's named fix priced before it was built, and refused at 0.730×.** `0110` left the item as a design question: the Annex B frame costs ~44 ns on **60.16%** of the corpus's calls, and the fix it pointed at was to move the per-invocation state off the function object onto a **thread-local invocation stack** — one write in, an index decrement out, no read-modify-write of a field every other call touches, with `caller`/`arguments` finding the innermost invocation by scanning. **An M–L with real correctness surface** (recursion, re-entrancy, a generator suspending mid-call), so it gets priced first. | Arm | ns over control | — control 0.36 ns; **save/restore on the function object, what the engine does today: 22.96**; **push/pop a thread-local stack, the fix: 16.77**; one 56-byte `Arguments` copy alone: **7.83**. **The saving is 6.19 ns — 221 ms of a 108 879 ms driver, 0.20% of the corpus, for an M–L.** ***And the third arm says why: the cost IS the copying, and relocating where the copying lands does not remove it.*** **So the lever is eliminating copies rather than moving them.** Removing the frame outright is **1.46%, seven times the relocation**, and that is where any effort belongs — and it is the hard one: a static gate on *"does this program ever touch `caller`/`arguments`"* is **unsound**, because both are reachable through a computed member access no analysis of the source can exclude, and a gate flipped at *first materialisation* gets the very read that flips it wrong. **A replica is the right instrument here, and it is worth saying why given `0108`** — that patch's finding was that replicating a *mechanism* misprices it, because the engine's own scopes have insides; **a struct copy has no inside**, and the JIT emits the same moves for the same layout wherever it appears. The **absolute** is still an under-estimate: these arms write static fields where the engine writes instance fields of a heap object, so the engine pays write barriers these do not — most likely the gap between 22.96 ns here and ~44 ns in situ. **The ratio is what the arm is for.** One new benchmark file and one CLI line; **no engine source touched**. **Apply after `0110`.** |
| `0110-js-legacy-frame-census` | `Broiler.JS` | **`0109`'s ceiling turned into a measurement, and two instrument defects `0103` surfaced and left now closed.** `0109` bounded the Annex B `caller`/`arguments` frame at **≤1.65%** by charging it to every call with a JavaScript callee, because nothing said how many of those callees carry the pair. **Counted at the entry it is 35 715 923 of 59 372 494 calls — 60.16% of all calls, 88.14% of the JavaScript-callee ones — worth 1 586 ms of a 108 879 ms driver: 1.46%.** The ceiling was tight and the item is real. **Richards pays it on 100.00% of its calls**, DeltaBlue 96.23%, Splay 96.12%, Typescript 75.44%, Box2D 69.87%, EarleyBoyer 50.53% — **and PdfJS 0.59%, Gameboy 0.02%**. *The two that escape are the two that are strict*, the same split the strict-transition census found from the other side: a strict function carries no legacy pair, so **a program written in strict mode does not pay this at all.** **It also settles a confound in `0109`'s attribution rather than leaving it inferred.** An arrow differs from an ordinary sloppy function in more than the legacy frame *if* it also skips the sloppy `this` coercion, so the probe now asks the engine which of the two each shape performs: **arrow and method both coerce and neither pushes a frame**, so they differ in exactly one thing and the control was sound — and on the corpus `thisCoercions` equals `legacyFrames` **to the call**, so a single "ordinary sloppy function" predicate gates both. **Two instrument defects closed.** The property-map policy table labelled `round-up-16` *"current: VirtualMemory.Allocate as written"* — **false since 2-7 shipped geometric-from-`NodeBlock`** — so the simulation named a baseline nobody runs while leaving the policy the engine actually uses unmarked; both labels are corrected against the code. And one histogram bucket read **−15**, whose cause is structural: the table counts each map once by **moving it out of the previous bucket** as it grows, which assumes its arrival there was counted, and a map already in that bucket when `Reset` zeroed the table has a decrement with nothing to cancel. ***A negative count is not a distribution, and every share is divided by a total containing it.*** The decrement now clamps at zero and the clamps are counted (`resetStraddlingMaps`): **17 this run, 0 negatives** — the defect reproduces and the fix is demonstrated rather than assumed. Compiler suite green: **1 267 tests**; storage suite **46**. **Apply after `0109`.** |
| `0109-js-call-bookkeeping-attribution` | `Broiler.JS` | **Item 4-5's missing cost attributed, and the sum closes to within 0.5 ns of its own target: 92% of a call's bookkeeping is Annex B `caller`/`arguments`.** `0108` measured the bookkeeping at ~50 ns of a ~115 ns call entry and named eight operations it could be in; **the isolating control needs no engine change either.** `AddLegacyCallerAndArguments` is emitted for ordinary non-strict function *declarations and expressions*, so an **arrow** and a **shorthand method** are sloppy callees with no legacy frame — and through the *same long entry* an arrow costs **71.79 ns against an ordinary function's 116.19**, only **3.81 ns** more than the short entry that skips everything. The components, measured against **the engine's own accessors** rather than replicas: **legacy frame 44.40 ns**, executing-function save/set/restore **2.14**, the `JSEngine.Current` cast and its `Options.ScriptHostMode` read **2.14**, the two `with`-scope pushes — the most suspicious-looking lines in the method — **0.01, free**. **Sum 48.69 against a 48.21 target.** So `PushLegacyFrame` copying the `Arguments` struct into a `LegacyFrame` and again into the function, then popping it back, **on every call to every ordinary non-strict function**, is the item: **≤1.65% of the corpus** bounded to calls that can pay it. *Item 2-9 already recorded that these cells cost something at function **creation**; they also cost 44 ns per **call**, which nothing had measured.* ***The largest single attributable cost in this engine's call path is a legacy web-reality feature no benchmark uses.*** **And the strict control the ablation started from found something else — larger per call and rarer.** A strict callee entered from sloppy code costs **102.87 ns and 224 bytes MORE**, not less, because `StrictModeScope` writes the strict-mode `AsyncLocal` on entry **and again on exit**; that file's own comment says the write happens *"only on a transition, so the common case is now a ThreadStatic read and a compare"* — **true of a uniformly strict or uniformly sloppy call graph and false at every boundary between them**, and 4-5 fixed the read side while leaving the write side resting on an argument about frequency nothing in the engine could check. **So it is counted** (`RecordStrictTransition`, inside the `changed` branch where the claim lives, behind the same off-by-default flag the call counting already uses): **2 813 191 of 59 372 513 calls cross — 4.74%, worth 289 ms of 108 767 ms, 0.266%** — with **nine of twelve suites never crossing at all** and **PdfJS crossing on 52.65% of its calls**, so the comment is right about the corpus and wrong about one suite in it. One counter, one census column, one benchmark extended; nothing that ships behaves differently on any setting. Compiler suite green: **1 267 tests**. **Apply after `0108`.** |
| `0108-js-call-entry-cost` | `Broiler.JS` | **Item 4-5 unblocked: 44% of a call entry is bookkeeping the engine's own short path already skips.** 4-4 named this as *"the first thing 4-5 should do"* — *"Two call paths exist in this engine and one is much shorter; pricing the difference between them…"* — **and it had never been done.** 4-5's component pass priced the prologue by **replicating** its mechanisms locally (five nested `using`s at **0.011 ns**, `try`/`catch`/`finally` at 0.73, a delegate invoke at 0.68), they summed to about **10 ns of a ~147 ns call**, and the item concluded that **~85% of a call's fixed cost is "unattributable from outside the engine"** and blocked itself on **a sampling profiler this container does not have**. ***A replica prices the mechanism and says nothing about what the engine's own scopes do inside themselves.*** `JSFunction.InvokeCallback` is the engine's own short path — the same `EnterRealm`, the same `SelectInvocationDelegate`, the same `this` coercion, **one `using` scope instead of five and none of the executing-function or legacy-caller bookkeeping** — so their difference is that bookkeeping *as the engine performs it*. **A natural ablation that has been shipping the whole time.** On the same callee with the same prebuilt `Arguments`: **`InvokeFunction` 114.60 ns against `InvokeCallback` 64.43 ns — 50.18 ns, 0.562×**, spreads 12.5% and 5.4%. ***So 44% of a call entry is bookkeeping, not 10 ns of it, and the item is not blocked on a tool.*** **Worth 2 979 ms of a 104 620 ms driver — 2.85% of the corpus over the twelve suites that run**, against **4-4's entire ceiling of 2.43%** and 4-2b's landed 0.83%, and it needs **no speculation, no guard, no tier and no fallback path**. **A lower bound in both directions that matter**: the short arm additionally pays the reflection delegate dispatch (0.68 ns, already priced) and a tail-call type test the long path handles in its own loop. **It is a budget, not a saving** — the bookkeeping serves `f.caller`, strict mode across a call boundary, realms and `with` — but it localises the missing 85% to **eight named operations**, with **`EnterRealm` excluded because it is in *both* arms**, which the replica pass could not have shown. **Both arms are asserted to return the same answer before anything is timed**, because a short path that quietly returned early would look exactly like the finding this exists to produce; neither allocates, so the 32 B per argument an emitted call site pays is outside the measurement by construction. **Reached by a delegate bound once through reflection, so no engine change is needed and nothing that ships is touched.** Round-robin, arms reversed on alternate rounds, ratioed within each round — `0107`'s harness. **Also re-reads two probe rows this campaign had gone stale on**: the `native-call` split 4-4 built and never reported says **~125 ns of a 166 ns call is the `InvokeFunction` half against ~40 ns for the compiled body's own prologue**, and the control loop item 3-5 was created to fix now reads **7.67 ns and 0 B against the recorded 33.77 ns and 32 B** — the box per iteration is gone, so 4-5's closing inference that the control points at 3-4 rather than at phase 4 retires with it. Purely additive: one new benchmark file and one CLI line. **Apply after `0107`.** |
| `0107-js-generic-operator-ceiling` | `Broiler.JS` | **Item 4-2's arithmetic half priced, refused by its own arithmetic, and the lead it points at closed with it.** The item's third clause — *"arithmetic → raw `double`/`int` where feedback says so"* — stayed open on one stated blocker: the numeric-vs-generic signal was left uncollected because the compiler already proves numeric-ness statically, so a runtime counter has to be defined against **that** to say anything new. **Defined that way the population is not "arithmetic" at all**: item 3-1's speculation is **on by default** and already takes **71.76% of candidate nodes** statically, needing no feedback, so what is left for a feedback-driven tier is 3-1's refusals — and they are one thing, **`NoSavingToMake` at 26.22%**, refused by a condition that asks whether the guarded form removes a *box*. **That argument is correct about allocation and says nothing about time**, which is what a tier-2 specialization sells. **The census the item rests on was another seven-suite figure.** `arithmeticBothNumbers` reads **100.00%**, quoted as *"the guard predicts"*; over the twelve suites that run it is **92.10% of 26 198 356**, and **the spread is the finding rather than the total** — **Splay 0.46%**, PdfJS 46.56%, against 100.00% on Box2D and NavierStokes. Third instrument to fall to what §4.2a found, and the first where the variance is *evidence for* the item's own per-site thesis rather than against it. **Then the instrument had to be fixed before it could be read.** The first harness ran each arm's samples consecutively and returned generic-arm spreads of **161%, 76% and 470%** against effects near 3× — §3.5's forbidden condition, produced by the probe rather than found by it — reporting `multiply-generic` at **39.00 ns** and `less-generic` at **20.67 ns** where round-robin with a blocking collection between samples reports **15.42** and **3.93**: *a 2.5× and a 5.4× error, both in the direction that would have founded the item*. Re-run with every arm once per round, reversed on alternate rounds, ratioed **within** each round: **multiply 0.704× (11/12 rounds, 6.39 ns saved)**, multiply-when-wrong 1.760×, **add 0.906×** whose **miss is 18.567× and 352 B** because a failed `+` guard is a real answer (string concatenation) rather than a coercion, **relational 0.753× (12/12)** whose miss is still **0.917×** — the one guard here with no losing side. **Multiplied out the item is refused: 124 ms of a 104 620 ms driver, 0.119%**, and **net negative at the add rate**. **4-2b landed at 0.83% and this campaign already called that below the noise floor**; this is 7× smaller. *(The driver is the **twelve suites that run**, per §4.2a's convention — a first reading divided by all fifteen and got 0.038%, because Mandreel spends 286 728 ms hitting the stack guard and is 72% of a fifteen-suite wall clock while contributing almost nothing to any count.)* **And the relational lead is closed rather than left as a guess**, which took the counter nobody had built — 3-1's gate excludes the relational operators and 3-5 needs one side already unboxed, so a comparison over two boxed operands is reached by **no fast path in the engine** — **23 986 595 comparisons, 99.85% both-Numbers** (far more uniform than arithmetic), worth **23 ms, 0.022%**. **So the whole generic binary-operator surface, 50.1 M invocations, is 497 ms of 104 620 ms — 0.475% of the corpus's execution time if removed entirely**, the third side of the box 4-2b's read path (≤ ~9%) and call path (≤ ~5.5%) already had. **The new counter is exactly-once by a re-entrancy guard rather than by a case analysis over which paths delegate** — `JSObject` coerces and calls the primitive's, `JSNumber` unwraps and calls the base's, the base hands a BigInt comparison to the other operand's mirror — because that analysis is what left **`BitwiseXor` unhooked and silent about it**. **Four fixtures assert exact counts**: N comparisons in the source are N in the counter, through the re-dispatching object path *where a naive hook double counts*, with a string comparison separating from a numeric one, and **across all four operators so a missing hook shows as three** rather than as a smaller number nobody questions. Hooks sit behind the existing off-by-default flag on the same methods whose arithmetic siblings already carry one; nothing that ships changes on any setting. Compiler suite green: **1 267 tests**. **Apply after `0106`.** |
| `0106-js-reentry-structural-partition` | `Broiler.JS` | **`0105`'s residual settled, and most of it turned out to be a defect in the comparison rather than in the mechanism.** `0105` reported **84.1%** of re-entered bodies reproducing the eager tree and said plainly what that number was worth — *a printed diff answers only “equal up to a renaming somebody thought of in advance”* — and the residual **15.9%** looked like ordinal divergence on every instance examined without that being proof. **The first finding is in the checker.** The gensym families were canonicalised in one pass whose ordinal table was keyed on the **bare number**, so `Context3` and `#TempJSValue3` shared an entry, and any function where two families drew the same number on one side and not the other **desynchronised every ordinal after it** — reporting a difference that was an artifact of the comparison. *The families do not share a counter; the table must not either.* **One table per family takes five of six corpora from 96.6–99.9% to 100.0%.** **Then the residual is partitioned rather than described.** Beside the equality up to an order-preserving renaming, the same three families are **erased** instead of mapped, which asks whether the trees agree in every token a counter did **not** produce: **5 723 of 5 723 on every corpus, 0 throwing** — so no re-entered body differs from its eager tree in a node, an operator, a constant or a shape. **Erasure is deliberately the weaker question and is reported beside the stronger rather than instead of it**, because what it hides is a *permutation* of counter values. **What is left is 471 functions whose ordinals differ, and they classify exactly two ways with NOTHING in “other”.** **460** are the site table's `-1`: the check compiles every body a second time and a retained context recompiles its **whole subtree**, so Typescript drives item 4-2b's process-wide counter from **24 759 to exactly its 65 536 cap**. **11** are the **eager** side reusing a site the re-entry allocated fresh — 4-2b's tier-2 rule working as designed, since a recompile for tiering re-uses the tier-1 site so the feedback it consumes stays addressable, and a re-entry has no tier-1 tree to re-use from. ***Both are properties of compiling the same body twice in one process, which a deferral by construction does not do.*** Per-process, so the corpora do not share the counter: closure **100.0%**, mandreel **100.0%**, jQuery **99.8%**, PdfJS **99.7%**, Box2D **99.7%**, Typescript **73.7%** — **91.8% over 5 723, with every one of the remaining 471 accounted for**. **Both equalities are pinned directly rather than trusted for never having failed** (§3.5's rule from `0096`, and the rule `0104`'s layout checker was built under): one fixture drives a counter renaming (**both accept**), a token no counter produced (**both reject**), and a site reused on one side only — which the **strong** equality reports and the **weak** one cannot, which is exactly why the residual is classified from the raw sequences instead of left inside the weaker number. Purely additive to behaviour; the retention switch is still off by default. Compiler suite green: **1 263 tests**. **Apply after `0105`.** |
| `0105-js-deferred-body-reentry` | `Broiler.JS` | **Item 1-1's remaining half: a nested body compiled from a kept enclosing scope, and shown to reproduce the tree the eager path builds.** The item names *one* obstacle — the capture layout — and `0104` settled it: derived from source alone it **never misses on 5 157 sites**. What is left is **the part the item does not name**. A deferred body must be compiled against the enclosing compiler's state, that state is **twenty-eight fields deep**, it sits on the least-exercised path in the engine, and every one of them is a **silent miscompile** if wrong. *Reproducing it is not a thing to attempt; keeping it is.* **The design rests on one fact about `FastFunctionScope`: it is not pooled.** Its `Dispose` only pops the stack, so a reference to it retains the whole `Parent` chain intact **after the `CreateFunction` frame that built it has returned**, and `LinkedStack.Switch` already re-enters an arbitrary scope. A retained context is therefore that reference plus **the five `FastCompiler` fields not reachable from the scope** — strictness, `ScriptInfo`, member-initializer state, parameter-initializer depth — restored on entry and put back in a `finally`. `CreateFunction` splits into a **wrapper that decides retention** and a `Core` both paths call; the wrapper **refuses all fourteen context-carrying parameters** rather than trying to save them, and refuses a body inside a `with`. **Refusing is the correct default**: an unretained function is compiled eagerly, exactly as before. **Nothing is deferred yet, deliberately.** The eager path still compiles every body; `BROILER_JS_DEFER_TREE=1` decides only whether a context is **kept**, and the mechanism compiles the body a **second** time and compares. That ordering is the whole point — **the comparison is only possible while both trees exist**, and once the eager one stops being produced there is nothing to check the deferred one against. **Nine fixtures**: capture, three-level transitive capture, per-iteration `let` cells, shadowing, both self-name shapes, strictness under `'use strict'` (whose loss `4-2a` records as turning a `ReferenceError` into a silent global), and **that re-entry leaves the compiler state it found** — a two-program fixture, because a scope stack left moved corrupts the *next* compilation in a way no fixture about the deferred function could see. **On the corpus, 4 811 of 5 723 retained functions (84.1%) reproduce character-for-character** after canonicalising four families of compiler-generated numbering — site indices and three gensym prefixes, which are **counters over a compilation** and so differ by construction on a second pass. Mandreel **100.0%**, PdfJS **99.9%**, Box2D **99.5%**, jQuery **97.0%**, Closure **96.6%**, Typescript **49.6%**. Each canonicalisation moved the number (**45.5% → 48.9% → 83.2% → 84.1%**) **without once exposing a structural difference** — evidence about the residual, *not proof*; settling it needs a structural comparison rather than a textual one. **The mechanism is still not the item.** What remains is suppressing the eager build and threading the deferred site through `Relay`, which `BLambdaExpression`'s **readonly `Body`** makes a change to the expression node rather than to the compiler — **item 1-1 stays L**. Off by default; retention changes no answer, asserted rather than assumed. Compiler suite green: **1 261 tests**. **Apply after `0104`; depends on `0102` and `0104`.** |
| `0104-js-capture-layout-checker` | `Broiler.JS` | **Item 1-1's remaining half: its layout validated on the corpus, and a soundness defect found in `0101`'s own code on the way.** The item is blocked on one stated thing — a captured name's `Box[]` index is decided by `LambdaRewriter` *from the tree*, and a deferred body has none — so the layout must come from source alone. `0101` built the walk; this builds the checker that decides whether the derivation is correct. **The property is asymmetric**: over-approximating costs a box per creation site and that name's numeric tier; under-approximating means a deferred body resolves a name to a box that is not there — *a miscompile*. **Eleven fixtures**, one of which deliberately records an empty prediction against a non-empty truth and asserts the miss is reported. **Two structural obstacles the item's statement does not name.** `ClosureRepository` holds **two** populations — bindings **handed in** (`Setup`, `index >= 0`) and the lambda's **own locals something nested captures** (`Convert`, `index == -1`) — so compared against the whole repository `function outer(){ var q = 1; var inner = function(){ return q; }; }` reports ***`outer` missing `q`***, a function missing its own local; **the deferral needs two derivations, not one**, and `FreeNameScan.ForProgram` composes bottom-up and can serve both. And **every function is handed a `ScriptInfo_*` binding no source identifier names**, so the layout needs a reserved region for compiler-introduced captures. **Then three corrections, in the order they had to be made.** *Repeats are recognised rather than counted* — a layout is a property of a site, **Mandreel relays 1 336 of its 1 358 sites twice**, and **repeat disagreements are 0 on every corpus**, so the repeat is duplication rather than the rewrite deciding differently. *Undeferrable bodies are excluded* — a defect in this checker: a `Dynamic` body has no set to be right about, but an empty prediction against a real capture set reports **every** capture as a miss, which was the whole `predicted{}` population on Mandreel, PdfJS and Typescript (7 sites). *And one real defect, in `0101`*: with the noise gone every remaining miss was the same shape — a function referencing its own name. `EnterFunction` bound the function's own name inside it **unconditionally**, under a comment describing only the named-function-**expression** case; a **declaration**'s name is bound in the **enclosing** scope, so a self-reference is free and a deferred body must be handed a box for it. **138 sites across five corpora reported as capturing nothing** — one condition, and soundness rather than precision. **With those three the corpus reports ZERO missed sites on 5 157 checked**, over-approximating on **2 712** of them (safe, and the cost side the mechanism must be measured against). **And the one shape the corpus does not contain is closed too**: a named function *expression*'s own name, which the specification binds inside and which **this engine materialises as a handed-in cell anyway**. A first attempt tested `Variable != null` — the field that binding leaves null on purpose, its own comment saying it *"is not a local Variable of this scope … exposed via `EvalCaptureExpression` only"* — which is `0097`'s rule a third time (*ask what the compiler BUILT, not what the analysis PROVED*) and **the first time it has decided a mechanism rather than a measurement**. Adding the self-name unconditionally cost **126 sites** of precision, so `FreeNameScan` now gives it **a scope of its own**, where the specification puts it too, so a parameter of the same spelling shadows it and a reference reaching it is distinguishable — **the gap closes at no precision cost, 2 445 exact either way**. **The deferral mechanism itself remains unbuilt and remains L.** The checker is off by default (`BROILER_JS_CAPTURE_LAYOUT_CHECK=1`); **the `FreeNameScan` fix is unconditional, because it is a correctness fix**. Compiler suite green: **1 252 tests**. **Apply after `0103`; depends on `0102`.** |
| `0103-js-widen-census-corpus` | `Broiler.JS` | **The corpus every phase-3 and phase-4 headline is computed over was 7 of Octane's 15 suites, and the reason had never been written down.** Item 2-13 established that this engine's largest *private* deficiency is **zlib — 12.0× behind Jint relative to Chromium** — so the next step was to ask what the censuses say about it. They say nothing: zlib is in none of them, and neither are Mandreel, Gameboy, PdfJS, Typescript, CodeLoad, Splay or RegExp. **`TypeFeedbackMetrics` and `SpecializingTierMetrics` both ran the same seven**, whose output the roadmap quotes as ***"the corpus"*** over a denominator it never states — while the third, `PropertyMapDistributionMetrics`, **lists all fifteen and reaches nine**, aborting on the same Mandreel overflow and producing *nothing at all* when it does, so items 2-7 and 2-9's map figures were **not reproducible from a clean tree**. *Three instruments, three different partial answers, one phrase used for all of them.* **Why seven is not a choice about cost**: widening the list aborts at the ninth suite with an **uncatchable .NET stack overflow** in Mandreel's `global_init`. Item 0-2's 16 MiB thread and 4 MiB reserve are a property of **the shell** (`Program.cs`), and the benchmark hosts built plain contexts on whatever stack the runtime handed `Main` — *every benchmark host in this campaign has been running without the reserve the shell has had since phase 0*, so the one suite that needs it was permanently unmeasurable. **And all three made finding out as expensive as possible**: each serialized once, at the end, so the abort discarded every suite before it. **All fixed** — all three run on the shell's thread with the shell's `MaxStackUsageBytes` and checkpoint after every suite, Mandreel now fails catchably instead of taking the process down, and `TypeFeedbackMetrics` **emits its corpus aggregate with the suite count beside it** rather than leaving it to be totalled by whoever quotes it — which is the step where the suite list stopped travelling with the number. **Both widened counts move a headline, and both reproduce their old value over the old seven**, so each is the same instrument over more suites rather than a different measurement: monomorphic reads **93.54% of 37.9 M → 80.11% of 307.9 M**, monomorphic calls **96.70% of 4.24 M → 86.35% of 52.9 M**, boxes allocated **31 401 346 → 90 641 738**, allocated bytes **3.13 GB → 12.93 GB**. **87.7% of the corpus's property reads and 65.4% of its boxes were outside the seven.** The suites that move them had never been counted: **Gameboy** is 34.60% monomorphic with **1 282 polymorphic sites** and allocates **41.3 M boxes on its own — 1.32× the entire previously measured corpus**; **Splay** is **10.15%** monomorphic; **Typescript** is 67% of the corpus's reads and allocates 4.96 GB. **What survives the widening is worth as much as what does not**: `0090`'s GC denominator holds at **1.80% against 2.29%**, so *"a box costs ~14× more to create than to collect"* was not an artifact of the corpus and §Non-goals' GC bullet stands — while phase 3's ranking of its own remainder, *"an XL bidding for under 2%"*, was sized against a driver a fifth the size of the real one. **Honest limits, stated in the output**: zlib (`read` builtin lives in the shell), Mandreel (now hits the guard) and RegExp (pre-existing checksum) report setup-only counts, so both headlines are over **12** suites and the JSON says so. **It also re-takes two landed phase-2 items their own instrument could not previously complete**: **2-9 is corroborated** at **2 202 782** maps on the full corpus, the right side of its recorded 16.2 M → 2.5 M; **2-7 splits**, its live-memory result still favouring the shipped policy but by less than recorded (**0.644×** against **0.56×**) while its **allocated-bytes win changes sign** — geometric growth pays **33×** the node copying (13 858 188 against 424 472) on suites 2-7 never saw, turning **0.82× into 1.044×**. The decision stands, taken on live memory; the allocated column should stop being quoted. **Two instrument defects surfaced rather than silently fixed**: the policy table still labels `round-up-16` *"current"* though 2-7 replaced it, and one histogram bucket reads **−15**, so `negativeBucketCounts` now ships in the output. **Purely additive to behaviour**: four benchmark-host files, no engine source touched, nothing that ships changes. **Applies to `14fa4f10`; independent of `0102`, though both were verified applying in sequence with `git am --keep-cr`.** |
| `0102-js-deferral-population` | `Broiler.JS` | **Item 1-1's remaining half: the population counted, and the shortcut that looked available refused by the counter built to test it.** The item defers a function body's expression-tree construction to first invocation — 33.6–63.9% of compile over a population **84–99.7% never invoked** — and names one obstacle: a captured name's index in the enclosing lambda's `Box[]` is decided by `LambdaRewriter` **from the tree**, and a deferred body has none. `0101` built and priced the walk that makes the layout addressable. **What nobody had counted is how many sites need the layout at all**: a function whose free names resolve to nothing an enclosing scope holds captures nothing, is handed no boxes, and is deferrable with the mechanism that already exists. **Counted, over 5 762 function sites: 728 are capture-free, 12.6%** — 7.4% on Mandreel, 39.7% on the flattest corpus. So there is no cheap subset to take first: **87.3% of sites need the capture mechanism**, which is the item. `Dynamic` — the direct-`eval` risk the item's text spends most of its words on — refuses **7 sites of 5 762, 0.1%**, the second time this item's stated risks have come in an order the measurement reverses. **And the reading that looked like an opening is refused.** Mandreel is 1 364 *top-level* declarations, and its 7 605 bound free names are only **165 function-owned**; a script's top-level `var` is a property of the global object per spec, so those looked deferrable for nothing. They are not — **`cellBacked` equals `bound` exactly on all six corpora, 15 118 of 15 118** — because this engine gives a program-level binding a CLR local in the program lambda like any other. ***A spec-level fact about where a binding lives is not a fact about where the compiler puts it.*** **The instrument was made to discriminate before it was pointed anywhere**, which is what makes that equality a finding rather than a claim about the counter: 18 fixtures whose load-bearing half is the negatives — the **same body text in two enclosing scopes gives two different verdicts** (no identifier scan can do that), a parameter or an inner `var` sharing the outer spelling captures nothing (the distinction `NestedFunctionScanner` cannot make), and `cellBacked` is shown to separate from `bound` *before* the equality is reported, via a named function expression's own name, which binds with no CLR local and reads **1 bound / 0 cellBacked** against an ordinary local's 1 / 1. Deliberately breaking the resolver fails **exactly the four capture-detecting fixtures** and leaves the other eleven green. **The denominator is cross-checked rather than asserted**: classified sites match `--compile-profile`'s own function count **exactly on four corpora**, and by **+2 and +1** on the two that evaluate a CodeLoad epilogue — which contains exactly 2 and 1 inline functions. One hazard avoided and pinned: the probe reads the enclosing scope through a new side-effect-free `TryResolveBinding` rather than `GetVariable`, because `GetVariable` sets `RootScope.HasOuterFunctionCaptures` and **that flag is a conjunct of the tiering gate** — a probe built on it would turn tiering *off* for functions it merely asked about; a fixture runs the same program on both settings of the switch. Off by default (`BROILER_JS_DEFER_TREE_COUNT=1`), a compile-time counter on the same terms as `0094`'s and `0097`'s, and it changes no emission on any setting. **Purely additive** — one new file, one new test file, plus counters and 30 lines of benchmark instrumentation, with no existing emission source touched. Whole compiler suite green: **1 241 tests**, +18 and every one of them this patch's. **Applies to `14fa4f10`, and depends on nothing else.** |

**`0102`–`0111` are pending against the `Broiler.JS` pinned pointer `14fa4f10`**, on the usual terms: the
push to `Broiler-Platform/Broiler.JS` returned **403** from the session's git proxy, so the pointer
is deliberately *not* bumped and every figure in their sections was measured on a local build of the
pin plus those patches. `0102` and `0103` are **independent of each other** — the compiler and its diagnostics against
four benchmark-host files — but **`0104` builds on `0102`**, whose side-effect-free
`TryResolveBinding` its prediction uses, and **`0105` builds on both** (it splits `CreateFunction`
around `0102`'s retention decision and its corpus figures are read beside `0104`'s), so
**`0102` → `0103` → `0104` → `0105` is the order**, verified by applying all four in sequence to a
clean checkout of the pin with `git am --keep-cr` **and diffing the result against the tree they
were generated from** — identical. None needs a
**main-repo fallback**: `0102`'s and `0104`'s counters and `0105`'s retention are off by default
(`BROILER_JS_DEFER_TREE_COUNT=1`, `BROILER_JS_CAPTURE_LAYOUT_CHECK=1`, `BROILER_JS_DEFER_TREE=1`) and
change no emission on any setting, `0103` touches only the benchmark host, `0107`'s
relational hooks sit behind `ArithmeticOperandDiagnostics.Enabled` — off by default, on the same
methods whose arithmetic siblings already carry one — `0108` adds one benchmark file that
reaches an existing `internal` method by reflection, `0109`'s strict-transition counter and `0110`'s
entry-shape counter sit behind `CallPathDiagnostics.Enabled`, `0110`'s histogram clamp only
changes an already-wrong output, and `0111` touches no engine source at all, so nothing CI runs
behaves differently with or without any of them.

## What has cleared

**The handoff has now completed a fourth time, and this time it took all three stacks at
once.** The fifteen files pending at the last reading — `0087` (`Broiler.HTML`), `0088`
(`Broiler.CSS`) and `0089`–`0101` (`Broiler.JS`) — have all been applied, pushed and their
pointers bumped.

They were checked **patch by patch against each submodule's log rather than inferred from this
prose**, which is the only reading of a pointer this campaign now trusts: each patch's `Subject`
resolved to a commit, that commit's own `format-patch` output was diffed against the patch file
(identical once the `From <sha>` line, the blob `index` lines and the trailing git version are set
aside — the whole of the difference on all fifteen), and the pointer each was pending against was
confirmed an ancestor of the new pin.

| Patch | Submodule | Landed as | Pending against |
| --- | --- | --- | --- |
| `0087-html-backdrop-painting-props` | `Broiler.HTML` | `2f94c0d5` | `29bf9c33` |
| `0088-css-nth-child-of-selector` | `Broiler.CSS` | `076ed5d5` | `dba36efb` |
| `0089-js-numeric-tree-order` | `Broiler.JS` | `12760bb9` | `cca39b4d` |
| `0090-js-gc-pause-accounting` | `Broiler.JS` | `48ad65e7` | `cca39b4d` |
| `0091-js-update-target-census` | `Broiler.JS` | `01c79c46` | `cca39b4d` |
| `0092-js-numeric-local-defeat-tests` | `Broiler.JS` | `2bab9775` | `cca39b4d` |
| `0093-js-3-8a-defeat-ab` | `Broiler.JS` | `16389682` | `cca39b4d` |
| `0094-js-speculative-numeric-population` | `Broiler.JS` | `e0bb9b40` | `cca39b4d` |
| `0095-js-speculative-numeric-storage` | `Broiler.JS` | `cfed00ef` | `cca39b4d` |
| `0096-js-speculative-numeric-read-paths` | `Broiler.JS` | `c2667c29` | `cca39b4d` |
| `0097-js-imported-outer-numeric-population` | `Broiler.JS` | `6ff52f3b` | `cca39b4d` |
| `0098-js-async-job-scheduling` | `Broiler.JS` | `ba31a4a9` | `cca39b4d` |
| `0099-js-execution-exclusion` | `Broiler.JS` | `b80327ac` | `cca39b4d` |
| `0100-js-blocking-host-wait` | `Broiler.JS` | `3fa35e14` | `cca39b4d` |
| `0101-js-free-name-scan` | `Broiler.JS` | `14fa4f10` | `cca39b4d` |

**So every figure recorded for those fifteen now describes the pinned pointers directly**, rather
than a local build plus a patch series applied in order, which is what their roadmap sections had
to say while they were pending. The `Broiler.JS` pin is now `14fa4f10`, `Broiler.HTML` `2f94c0d5`
and `Broiler.CSS` `076ed5d5`.

The fifty-two patches this file carried before them (`0001`–`0086`, in seven earlier bumps) had
already cleared the same way.

**A 403 has now meant *deferred* rather than *stranded* four times running**, and what makes that
safe is unchanged: the pointer is never bumped locally, so nothing written here can name a commit
CI cannot clone. The renumbering note the last series carried — `0087`–`0099` written, `+2` shifted
once `main` landed the HTML and CSS pair on the same two numbers — is retired with the series, but
the reason it happened is not, and it is the one thing worth keeping from it: **`patches/` is one
flat namespace across every submodule**, so two branches numbering from the same high-water mark
collide whenever both are open, which is the ordinary case rather than an unlucky one. Number from
`git ls-files patches/` at the moment of writing, and re-verify the chain after any rename.
