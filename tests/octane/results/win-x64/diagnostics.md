# Octane failure diagnostics

- Generated: `2026-08-07T12:55:06.809Z`
- Platform: `win-x64`
- Broiler: `Broiler.JS (BroilerJS --script-host)` — per-suite timeout 1800s (floor; a suite may raise its own — the budget each ran under is in its section below), 3 repetitions
- Jint: `Jint 4.15.3 (interpreter)` — per-suite timeout 1800s (floor; a suite may raise its own — the budget each ran under is in its section below), 3 repetitions

Statuses: **error** — Octane caught the throw and scored the rest;
**crash** — the process died and took the suite with it;
**timeout** — no result within the per-suite timeout.

## Broiler

1 of 15 suites did not complete.

| Suite | Status | Failing type | Where |
|---|---|---|---|
| [Mandreel](#broiler-mandreel) | error | `RangeError` | benchmark `Mandreel`, phase `Setup` |

### Broiler: Mandreel

- **Status**: error (exit code 0)
- **Failing type**: `RangeError`
- **Where**: benchmark `Mandreel`, phase `Setup`
- **Files evaluated**: base.js → mandreel.js → octane-runner.js
- **Ran for**: 503.4s
- **Full output**: `tests\octane\logs\broiler\Mandreel.rep1.log`

```text
Maximum call stack size exceeded
```

**JavaScript stack** — mapped back to the Octane sources:

```text
    at EnsureWithinStackBudget (/_/Broiler.JS/Broiler.JavaScript.Engine/CallFrames.cs:215)
    at mandreelAppInit (mandreel.js:1460)
    at mandreelAppStartRenderWebGL (mandreel.js:1763)
    at _mandreelAppStartReady (mandreel.js:1438)
    at _mandreelAppAudioReady (mandreel.js:1449)
    at setupMandreel (mandreel.js:56)
    at RunNextSetup (base.js:356)
    at RunStep (base.js:157)
    at inline (base.js:173)
    at __octaneRun (octane-runner.js:217)
    at native (octane-runner.js:225)
```

Re-run just this suite:

```bash
./scripts/run-octane-benchmarks.sh --engines broiler --skip-build --only Mandreel --verbose
```

## Jint

All 15 suites completed. Nothing to diagnose.

---
_Generated by `scripts/run-octane.mjs`. Re-runs of a single suite write to `tests/octane/logs/partial/`._
